import Layout from '@theme/Layout';
import type { ReactNode } from 'react';

export default function Home(): ReactNode {
  return (
    <Layout title="Knowledge Navigator">
      <main>
        <h1>Knowledge Navigator</h1>
      </main>
    </Layout>
  );
}

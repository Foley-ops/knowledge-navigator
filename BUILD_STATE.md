# Knowledge Navigator Build State

Resume point for any agent continuing this build.

**Build authority:** [`KNOWLEDGE_NAVIGATOR_BUILD_RUNBOOK.md`](./KNOWLEDGE_NAVIGATOR_BUILD_RUNBOOK.md).
That file — not this one — holds the product brief, the frozen decisions (§2), the
contracts all code must obey (§4), every checkpoint, and the Definition of Done (§6).
Frozen decisions are deliberately **not** copied here; read them from the runbook so
there is exactly one authority and no chance of drift.

**Project root:** `/Users/nick/Projects/knowledge-navigator` (the folder containing the
runbook). The build never reads or writes parent directories.

**How to resume:** read the runbook in full, then read this file. Start at the first
checkpoint not listed under *Completed checkpoints*, honouring its **Depends on** line.

## Current checkpoint
F09 complete — Phase F done, G00 next.

## Completed checkpoints
- **A00** — Inventory. Project root /Users/nick/Projects/knowledge-navigator contained only KNOWLEDGE_NAVIGATOR_BUILD_RUNBOOK.md and an empty .git/ (branch main, zero commits). No unrelated user files at risk; nothing outside this folder is read or written.
- **A01** — Service boundary created in place: apps/{api,web}, content/concepts, data, generated, nginx, packages/core, schemas, scripts. .gitkeep added to data/ and generated/ only. No nested knowledge-navigator/ child directory.
- **A02** — BUILD_STATE.md created with the six required sections; frozen decisions referenced by link to the runbook rather than copied.
- **A03** — .gitignore, .dockerignore and .npmrc written. Git ignores .env, node_modules, build output, coverage, all SQLite/journal/WAL files, logs and local model data, while keeping .env.example, package-lock.json, generated/graph.json and apps/web/sidebars.generated.ts trackable. The Docker context additionally excludes .git, the runbook, BUILD_STATE.md, host node_modules, data/, test artifacts and model data.
- **B00** — Root npm workspace created: package.json (private, type=module, engines >=22 <23, workspaces apps/* and packages/*, scripts validate/compile/test/typecheck/build/check/dev plus lint, format and smoke), tsconfig.base.json with strict settings, .prettierrc.json, .prettierignore and a flat eslint.config.js. 151 root dev packages installed with exact versions; package-lock.json committed.
- **B01** — @navigator/core workspace created with build (tsconfig.json) and typecheck (tsconfig.spec.json) configurations and empty exported modules schema.ts, loader.ts, validate.ts, compile.ts, query.ts, cli.ts and index.ts.
- **B02** — @navigator/api workspace created: Fastify 5 application factory buildApp() with zod-validated environment configuration for every variable in runbook §4.4, structured JSON logging that redacts question and context, a temporary GET /api/health returning {status:"ok"}, and a server entry point with SIGTERM/SIGINT shutdown. Root vitest.config.ts added.
- **B03** — @navigator/web scaffolded as a Docusaurus 3.10.2 classic TypeScript site with no tutorial blog or sample docs. Every @docusaurus/* dependency is exactly 3.10.2; blog disabled; title 'Knowledge Navigator'. remark-math 6.0.0 + rehype-katex 7.0.1 + katex 0.16.47 configured, with KaTeX CSS imported from the installed package (46 KaTeX font files emitted into the local bundle, no CDN). Docs plugin reads ../../content/concepts and uses sidebars.generated.ts. markdown.format='detect' so canonical .md is parsed as CommonMark and can never execute MDX imports.
- **B04** — MIT LICENSE added and README.md written: product purpose, an explicit personal/experimental/localhost warning, current status pointing at BUILD_STATE.md, the canonical-vs-disposable data split, requirements, both Docker Compose start commands, http://127.0.0.1:3000, a data-location table, the privacy defaults, source commands and a link to the runbook.
- **Phase A** — Committed as 297300b 'Phase A: establish the standalone project'. Staged diff inspected: no secrets, no .env, no database files, no node_modules.
- **Phase B** — Committed as 7833113 'Phase B: scaffold the monorepo'. Working tree clean.
- **C00** — packages/core/src/schema.ts implements runbook §4.1 executably with Zod 4: all 12 kinds, tiers 1-3, 5 review states, 21 relationship types, strict objects that reject unknown top-level and nested keys, lowercase dotted identifiers, /concepts/kebab-case slugs, absolute http(s) URLs, ISO calendar dates, and category paths rooted in one of the three atlas areas. Cross-field rules: primary_category must appear in categories, the slug name must match the concept_id tail, aliases must be unique once normalised, no self-relationships, no duplicate source_id on a page. Unrecognised keys are reported one diagnostic per key so every issue names an editable field.
- **C01** — packages/core/src/json-schema.ts generates schemas/concept.schema.json from the same Zod schema used at runtime via z.toJSONSchema(draft-2020-12). Object keys are sorted recursively, so regeneration is byte-stable. The document records which cross-field rules JSON Schema cannot express and names 'navigator validate' as authoritative. Exposed as 'navigator schema' / 'npm run schema'.
- **C02** — packages/core/src/loader.ts loads one concept file and returns file name, absolute path, validated metadata, raw body, searchable plain text, heading list, outgoing links with line numbers, raw HTML blocks, and a newline-normalised SHA-256 content hash. Markdown is parsed with mdast (gfm + math extensions) and never evaluated; YAML is parsed with schema 'core', maxAliasCount 0 and uniqueKeys, so aliases and duplicate keys are refused. Math is excluded from the searchable body; HTML is captured as data, never as markup.
- **C03** — packages/core/src/validate.ts enforces the page template by tier. Tier 1 requires all twelve §4.1 level-2 headings exactly once and in order, forbids any other level-2 heading and any level-1 heading (the title comes from frontmatter), and requires at least one source and one typed relationship. Tier 2 requires a non-empty body, at least one source, and at least one relationship or category. Tier 3 requires only valid metadata and a summary. Raw HTML is rejected at every tier.
- **C04** — validateCorpus() plus loadCorpus() detect errors spanning several files: duplicate concept_id, duplicate slug, aliases or titles colliding once normalised across concepts, relationship targets absent from the corpus, self-relationships, primary_category missing from categories, one source_id describing two different sources (title/url/source_kind must agree; supports and checked_on may legitimately differ per citation), file names disagreeing with slugs, relative concept links whose target file does not exist, and links escaping the content directory. Every rule runs in one pass and diagnostics are sorted by file, then field, then message. A deterministic corpus hash is computed over sorted file names and content hashes.
- **C05** — Eleven-page vertical slice authored in the runbook's order: convolution, cross-correlation, receptive-field, translation-equivariance, convolutional-layer, pooling, backpropagation-through-convolution, lenet, residual-connection, vgg, resnet. A scripted audit confirms every concept_id, slug, alias list, category list, primary category (set by category order) and prerequisite list matches the C05 identity table exactly, and that all four additional typed relations exist (cross-correlation contrasts_with convolution; convolutional-layer implements cross-correlation; resnet implements residual-connection; resnet contrasts_with vgg). No person, paper or stub concept nodes were created. Every source URL was checked with a bounded request.
- **C06** — AGENT_CONTENT_CONTRACT.md written: agents propose diffs and never publish or merge; concept_id and slug are permanent; the executable schema in packages/core/src/schema.ts is the format authority; every claim must trace to a source whose 'supports' names the section; uncertainty must be stated rather than smoothed over; validation must pass before proposing; an agent may never promote its own work above generated-draft. Includes the full Tier 1 page template, per-section writing guidance, linking rules, a review-state authority table, and a 13-item pre-publication checklist.
- **D00** — packages/core/src/db.ts defines the compiled schema (§4.2): concepts, aliases, categories, concept_categories, relationships, sources, concept_sources, build_meta and the concepts_fts virtual table, all STRICT where SQLite allows it, with foreign keys on and indexes on alias normalisation, category parents, relationship endpoints and source citations. compileCorpus() writes to a temporary path, records schema_version 1, corpus_hash, built_at, concept_count and generator in build_meta, and derives built_at from SOURCE_DATE_EPOCH when set.
- **D01** — Concept identity, body, plain text, content hash and source path are inserted, and titles plus aliases are inserted in normalised form with an is_title flag. Names that normalise alike within one concept are deduplicated; a collision between different concepts fails compilation, enforced by corpus validation and by a unique index on aliases.normalized.
- **D02** — Categories are inserted with a row for every path prefix (so 'Artificial Intelligence' is a navigable parent), each with name, top_level, depth and parent_id. concept_categories preserves declared order and flags exactly one primary category, set by category order. Relationships are inserted with type, endpoints, note, condition and position.
- **D03** — Sources are deduplicated by source_id into one row per distinct work (title, url, source_kind), inserted in id order for determinism. Citations live in concept_sources with the supports list as JSON and a per-citation checked_on, so one work can be cited by several concepts for different sections on different dates. Conflicting duplicates are rejected before any database is written.
- **D04** — concepts_fts populated with title, aliases, summary and plain-text body. searchConcepts() ranks exact title, exact alias, title prefix, alias prefix, then BM25 relevance with column weights 10/8/4/1, reports a plain-language rank explanation and the alias that matched, caps the limit at 50, and rejects empty or over-long queries. Every query token is quoted (and internal quotes doubled) before it reaches FTS5, so operators typed by a user arrive as literal text; LIKE wildcards are escaped in the prefix band.
- **D05** — generated/graph.json holds sorted nodes, edges, categories, build timestamp, schema version, corpus hash and counts. Nodes carry title, slug, kind, tier, review state, summary, aliases and categories so the browser can render a card without the API; edges carry type, note, condition and a stable id; categories form a tree including intermediate areas, with both conceptIds and primaryConceptIds.
- **D06** — apps/web/sidebars.generated.ts is generated from the compiled database: the primary category holds the canonical doc entry and every other category a ref entry to the same document, Tier 3 is omitted, and categories and entries are sorted by explicit label.
- **D07** — Compilation is atomic and verified. The database is built at a temporary path, then PRAGMA foreign_key_check, PRAGMA integrity_check, row counts against the corpus, and exactly-one-matching-primary-category per concept are all checked before the rename. Graph JSON and sidebars are written only after the rename succeeds.
- **D08** — navigator validate, compile, inspect <concept-id-or-slug> and search <query> implemented, with --content, --database and --limit options and a clear message when no compiled index exists. Root npm scripts validate, compile, inspect, search, schema and sidebars wired to them.
- **E00** — apps/api/src/index-handle.ts opens the compiled database read-only and refuses anything it cannot serve safely: missing file, unreadable file, missing required tables, no recorded schema version, or a schema version newer than this API understands. Each refusal names the cause and the fix. server.ts exits 1 with that message rather than starting; buildApp() still constructs so the health endpoint can report the state.
- **E01** — GET /api/health reports API status, database availability (with reason and message when unavailable) and assistant provider availability, returning 503 when degraded. GET /api/build reports schema version, corpus hash, build time, generator, counts and per-tier and per-review-state breakdowns. Neither leaks the database path, the Ollama URL or any environment variable name.
- **E02** — GET /api/concepts/:conceptId and GET /api/concepts/by-slug return metadata, aliases, categories, typed relationships in both directions, sources with their supported sections, canonical Markdown source and provenance (source path plus content hash). Markdown is returned as source; the server never renders HTML.
- **E03** — GET /api/search validates query length and limit, returns concept id, title, slug, summary, kind, tier, review state, the alias that matched and a plain-language rank explanation.
- **E04** — GET /api/graph/:conceptId traverses typed edges in both directions to depth 1-3, deduplicates nodes and edges, caps the result at 200 nodes, reports truncation and the cap, and labels each node with its distance from the centre. Depth outside 1-3 is refused, so no request can pull the whole corpus.
- **E05** — Provider interface, request type, structured result type and provider-status type defined in apps/api/src/assistant/types.ts, with the result contract expressed as a Zod schema. The disabled provider explains that browsing and search still work; the fixture provider is deterministic, builds its answer only from retrieved material, and never emits a citation it was not given. fixture is refused when NODE_ENV=production.
- **E06** — Grounded retrieval selects at most eight concepts deterministically: concepts whose title or alias appears verbatim in the question win first (longest phrase first), then the context's exact names, then full-text relevance over the question and context. Each selected concept carries its one-hop relationships and its sources, and material is fitted to a configurable character budget (ASSISTANT_CHARACTER_BUDGET, default 14000). Truncation from either the eight-concept cap or the budget is reported. The raw research context is never logged; only the derived search terms are returned.
- **E07** — The ollama provider posts a non-streaming, temperature-0 request to /api/chat with a JSON-schema format constraint, and maps every failure to a specific safe error: connection refusal to provider_unreachable, a slow model to timeout, a missing model to model_unavailable, a non-JSON or empty reply to invalid_model_output, a contract violation to invalid_model_output, and any citation or route concept id that was not supplied to fabricated_citation. The prompt states that retrieved text and researcher context are data rather than instructions, requires the response contract, requires explicit uncertainty, prohibits fabricated citations, lists the citable ids, and forbids claiming a method applies when required information is missing.
- **E08** — GET /api/assistant/status and POST /api/assistant/query implemented. Input lengths and enums are validated with field-level messages. Concept citations resolve to slugs and source citations to URLs. The retrieved canonical concepts and sources are returned on every response, including every failure, so a failed model never costs the researcher the evidence. Every response carries a request id, and only counts — never the question or context — are logged.
- **E09** — Safety limits: 64 KiB body limit, 180 s request and connection timeouts, per-process rate limiting (600 per minute overall, configurable), helmet security headers with a deny-all CSP for the JSON API, an explicit localhost origin allow-list, a single structured error shape with a request id, and a production error handler that returns no message detail and no stack trace.
- **F00** — Visual shell built around one idea: epistemic status is structural, not a badge. A thin 'evidence rail' down the left of every knowledge block encodes review state as texture (dashed for generated-draft, solid for source-checked, doubled for expert-reviewed, dotted for disputed), and the same signal recurs on concept pages, search results, retrieved evidence and answer sections. Palette is a cool instrument teal on a faintly green-grey ground, no shadows anywhere, separation by rule and ground. System sans for interface, a local serif (ui-serif/Iowan/Georgia) for prose, monospace reserved for stable identifiers where it carries meaning. Reading column 52rem, tool pages 78rem, transitions 120ms, reduced motion respected, visible focus ring on every interactive element. No externally hosted font, script, analytics or image.
- **F01** — Home page states the four jobs as questions in the researcher's own voice — 'What is this idea, exactly?', 'I'm stuck. What should I try next?', 'Which of these should I use?', 'How do these ideas connect?' — each a large serif line that is itself the link, with a plain one-line explanation. A search box sits above them and a trust note below says plainly that every page is currently a generated draft.
- **F02** — The Docusaurus document layout is wrapped so a canonical concept page carries a provenance strip above the title (review state with its meaning, kind and tier, aliases, categories with the primary marked, concept id) and, below the prose, its typed relationships with notes and conditions, a bounded neighbourhood, and its sources with the sections each one supports and the date its link was checked. Relationship targets link to their canonical pages. Frontmatter is read defensively: unknown shapes are dropped rather than guessed, and only http(s) source URLs are rendered.
- **F03** — Search page calls /api/search with a 200 ms debounce, cancels the in-flight request on every keystroke, and handles idle, working, empty, unavailable-index and API-failure states distinctly. Every result shows the review state, the summary and the plain-language reason it matched.
- **F04** — Explore renders the atlas from generated/graph.json at build time rather than through the API, so browsing survives an API outage. It is an indented outline, not a tile grid: top-level areas with concept and category counts, nested category paths, and concept entries carrying the evidence rail, the review state and the summary. Each entry that belongs to more than one category says so explicitly — 'Also filed under X — same page, one address'.
- **F05** — Concept neighbourhood built with Cytoscape.js inside a client-only component. Edges are labelled with their relationship type in words, the centre node is distinguished, clicking a node recentres, depth 1-3 is selectable, and truncation is reported. A complete text list of every edge sits below the canvas at all times — it is the accessible representation, not a last resort — together with keyboard-reachable buttons to recentre on any neighbour. If Cytoscape fails to load or the API is unavailable, the page says so and the declared relationships remain visible.
- **F06** — Ask form with question and optional context fields, four modes and three depths as pressed-state buttons, defaulting to unstick and intuitive. A privacy note names the configured local provider and its model. Validation prevents an empty question, and a recoverable failure leaves the question and context untouched in the form. The request is cancellable while it runs.
- **F07** — Structured assistance rendered as separate sections for interpretation, answer, candidate routes, assumptions, disqualifiers, missing information, next checks, citations and confidence. Concept citations link internally, source citations link externally, and a citation with no resolvable address is labelled 'no address on file' rather than shown as a working link. Repeated citations are de-duplicated. Uncertainty is visually explicit: assumption and missing-information sections use the dashed caution rail, disqualifiers a red rail, and confidence is a three-segment instrument reading with the reason spelled out. Empty sections say what their emptiness means rather than disappearing.
- **F08** — The answer can be re-run at another depth from buttons below it. The question, the context and the mode are preserved; only the depth changes.
- **F09** — Trust and failure states are consistent across the interface: review-state badges carry their meaning as a title everywhere they appear; an unavailable model, a generation failure, a fabricated-citation rejection, an unavailable index, an unreachable API, a truncated graph, a truncated retrieval, a citation with no address and low confidence each have their own honest wording. No failure is ever replaced with invented content.

## Last successful checks
- **A00** `pwd && /bin/ls -la && git status --short` → root confirmed; git works; only the runbook untracked
- **A01** `find . -maxdepth 3 -type d -not -path './.git*' | sort` → 17 planned directories, no nested project folder
- **A02** `read BUILD_STATE.md` → next checkpoint identifiable as A03 from this file plus the runbook
- **A03** `git check-ignore -q <path> for 12 paths` → 7/7 ignored as required, 5/5 kept trackable as required
- **B00** `npm install && npm ls --depth=0` → install succeeded; npm ls exited 0 with 11 top-level dev dependencies
- **B01** `npm run typecheck --workspace @navigator/core` → exit 0
- **B02** `npx vitest run apps/api/tests/health.test.ts` → 1 test passed — in-memory app.inject() returned HTTP 200 and {status:'ok'}
- **B03** `npm run build --workspace @navigator/web (with a temporary placeholder concept)` → build succeeded; routes /, /search, /explore, /ask, /about and /concepts/placeholder emitted; placeholder then removed
- **B04** `scripted audit of every fenced command and relative link in README.md` → 10/10 commands defined by package.json scripts or runbook §4.6; 4/4 local links resolve
- **Phase A+B** `npm run format:check && npm run lint && npm run typecheck && npm test && npm run build --workspace @navigator/web` → all clean; 1 test passing; Docusaurus production build succeeded
- **C00** `npx vitest run packages/core/tests/schema.test.ts` → 48 tests passed — one valid object plus at least one invalid example for every enum and format rule
- **C01** `npm run schema twice, then diff` → 5905 bytes; second run produced no diff (deterministic)
- **C02** `npx vitest run packages/core/tests/loader.test.ts` → 17 tests passed — valid file, missing frontmatter, unterminated block, empty block, invalid YAML, duplicate YAML keys, YAML alias refusal, invalid metadata, non-mapping frontmatter, code fence remaining data (including a frontmatter-shaped fence that cannot become metadata), raw HTML captured not interpreted, platform-stable hash, and disk read errors
- **C03** `npx vitest run packages/core/tests/validate-page.test.ts` → 16 tests passed — a passing and a failing page for each of Tier 1, Tier 2 and Tier 3
- **C04** `npx vitest run packages/core/tests/validate-corpus.test.ts` → 25 tests passed — every rejection case plus multi-error single-run reporting and deterministic ordering
- **C05** `npm run validate` → 11 concepts, 11 Tier 1, 11 generated-draft, 4 categories, 20 relationships, 95 internal links, 19 distinct sources, 40 source citations, 0 errors
- **C05** `scripted audit against the C05 identity table` → 11/11 files match concept_id, slug, aliases, categories, primary category and prerequisites; 4/4 extra typed relations present; exactly the eleven named concepts exist and nothing else
- **C06** `scripted consistency audit of the contract against schemas/concept.schema.json and packages/core/src/validate.ts` → 20/20 checks pass: enumerations, review-state table and Tier 1 heading template all match the executable definitions; every required obligation present; no permission to publish, promote or skip validation; links resolve
- **D00** `npx vitest run packages/core/tests/compile.test.ts` → 8 tests passed — empty valid corpus compiles, sqlite_master contains all 9 required tables, foreign_keys pragma is on, schema version is 1, all required indexes exist, invalid corpus writes no database, no temporary file remains
- **D01** `npx vitest run packages/core/tests/compile.test.ts + acceptance-corpus.test.ts` → all eleven concepts and their aliases compile; lookups for 'conv layer', 'CONV LAYER', 'convolution layer', 'ResNet', 'Residual Network', 'LeNet-5', 'RF' and eight more resolve to the right concept; a cross-concept name collision fails the build and writes no database
- **D02** `npx vitest run (compile + acceptance corpus)` → every relationship endpoint joins to a concept and PRAGMA foreign_key_check is empty; from ResNet, residual-connection is one hop and vgg, convolutional-layer and pooling are within two hops; all four runbook-named typed relations are present
- **D03** `npx vitest run (compile + acceptance corpus)` → one sources row for a work cited twice with two distinct citations; ResNet resolves to arxiv.org/abs/1512.03385, VGG to 1409.1556 and LeNet to ieeexplore 726791, each with its supported sections; a conflicting source_id fails the build
- **C05** `adversarial technical review of all eleven pages (35 agents: one independent reviewer per page, then two refutation-biased verifiers per serious finding)` → 12 serious findings raised; 11 upheld unanimously and all 11 corrected; 1 split finding also resolved by the same rewrite. Corpus revalidates with 0 errors and 138 unit tests pass.
- **D04** `npx vitest run packages/core/tests/search.test.ts` → 29 tests passed — ResNet, conv layer (and CONV LAYER / Conv-Layer), translation invariance, body phrases, prefix matching, determinism, limits, and 12 injection-shaped inputs that neither throw nor change the database
- **D05** `npx vitest run packages/core/tests/graph.test.ts` → 8 tests passed — two builds with SOURCE_DATE_EPOCH set are byte-identical; without it only builtAt differs; ordering, counts, edge endpoints and one-primary-category-per-concept all verified
- **D06** `npx vitest run packages/core/tests/sidebars.test.ts && npm run build --workspace @navigator/web` → 7 tests passed and the Docusaurus production build succeeded. ResNet, VGG, LeNet, receptive-field and translation-equivariance each appear as exactly one doc plus at least one ref; 11 concept URLs emitted, one per concept, alongside /, /search, /explore, /ask and /about. (Substituted for the runbook's 'gradient descent' example, which is not one of the eleven frozen concepts.)
- **D07** `npx vitest run packages/core/tests/compile.test.ts` → 23 tests passed — after a successful build, introducing a broken relationship target (and separately an unparseable file) fails compilation, leaves the previous database byte-identical and still usable, leaves the previous graph.json unchanged, and removes the temporary file
- **D08** `all four commands run against the acceptance corpus, plus npx vitest run packages/core/tests/cli.test.ts` → 11 tests passed driving the built dist/cli.js as a real process. validate: 11 concepts / 11 Tier 1 / 11 generated-draft / 0 errors. compile: 11 concepts, 25 names, 6 categories, 16 category links, 20 relationships, 19 sources, 40 citations, 11 FTS rows, three artefacts written. inspect resnet: full metadata, two categories with the primary starred, four typed relations with notes and conditions, four sources. search 'conv layer': Convolutional Layer first as exact-alias.
- **Phase D** `npm run format:check && npm run lint && npm run typecheck && npm test` → all clean; 197 tests passing across 11 files
- **E00** `npx vitest run apps/api/tests/index-handle.test.ts` → 14 tests passed — read queries work; UPDATE, DELETE, INSERT, DROP and CREATE all fail with a readonly error and leave the 11 concepts untouched; every refusal case reports the right reason
- **E01** `npx vitest run apps/api/tests/health.test.ts` → 6 tests passed for both healthy and missing-database states, including explicit assertions that no configuration secret appears in either response
- **E02** `npx vitest run apps/api/tests/read-endpoints.test.ts` → id and slug lookups return identical bodies; unknown values return 404; malformed ids and slugs return 400; a missing index returns a structured 503
- **E03** `npx vitest run apps/api/tests/read-endpoints.test.ts` → title, alias and body matches all covered, plus empty, missing, over-long, over-limit and eight injection-shaped queries; the corpus is verified unchanged after each hostile query
- **E04** `npx vitest run apps/api/tests/read-endpoints.test.ts` → depth behaviour, monotonic growth, distances, dedup of the two ResNet-to-residual-connection edges, edge endpoints always inside the node set, stable ordering across calls, unknown and malformed ids, and six rejected depth values
- **E05** `npx vitest run apps/api/tests/assistant.test.ts` → production rejection, non-production acceptance, ollama allowed in production, disabled provider behaviour, fixture determinism, and fixture citation containment all verified
- **E06** `npx vitest run apps/api/tests/assistant.test.ts` → determinism, alias preference, longest-name preference, context use, the eight-concept limit, budget enforcement, one-hop relationships and sources, and an empty result when nothing matches
- **E07** `npx vitest run apps/api/tests/ollama.test.ts` → 21 tests passed against a real mock HTTP server: success, fenced-JSON recovery, request shape and prompt content, connection refusal, timeout, missing model, server error, non-JSON reply, transport-level bad JSON, empty reply, six schema-invalid bodies, fabricated concept citation, fabricated source citation, fabricated route concept, and five status-probe cases
- **E08** `npx vitest run apps/api/tests/assistant.test.ts` → 43 tests passed — all four modes, all three depths, defaults, every contract field present, citation resolution, eight invalid inputs, provider-disabled failure carrying the evidence, index-unavailable 503, an honest empty-retrieval answer, and a prompt-injection attempt in the context that produces no fabricated citation
- **E09** `npx vitest run apps/api/tests/safety.test.ts` → 15 tests passed — oversized body returns 413, rate limit returns 429 with a retry hint and headers, allowed and disallowed origins behave correctly, helmet headers present and x-powered-by absent, structured 404, production 500 hides the message and any stack, development keeps the message but still no stack
- **Phase E** `npm run format:check && npm run lint && npm run typecheck && npm test` → all clean; 330 tests passing across 16 files
- **F00** `npm run build --workspace @navigator/web, then Playwright screenshots of all six routes at 1280x900 and 360x780, light and dark` → build succeeded; no console errors and no horizontal overflow at any viewport. Display mathematics was found overflowing 160px at 360px and is now confined to its own scroll container.
- **F01** `Playwright keyboard sweep from the home page` → /explore, /search, /ask and /about are all reachable by Tab alone, and each route returns 200
- **F02** `browser inspection of /concepts/resnet and /concepts/convolutional-layer` → all five fact groups and all three sections present; every internal link resolves (the Docusaurus build runs with onBrokenLinks and onBrokenMarkdownLinks set to throw)
- **F03** `browser journey: type 'conv layer' on /search, then open the first result` → first result is Convolutional Layer, matched as an exact alias, and clicking it lands on /concepts/convolutional-layer
- **F04** `browser inspection of /explore` → ResNet, VGG, LeNet, Receptive Field and Translation Equivariance each appear under both of their categories and every occurrence links to the single canonical URL. (Substituted for the runbook's 'gradient descent' example, which is not one of the eleven frozen concepts.)
- **F05** `browser inspection of /concepts/resnet` → the diagram renders with no server-side rendering error, the text list is populated, and an API failure produces an explanatory state rather than an empty panel
- **F06** `browser journey on /ask` → submitting with an empty question shows an inline error and sends nothing; after a failure the entered text is still present; the cancel button appears only while a request is in flight
- **F07** `browser journey against the fixture provider, then against the disabled provider` → all nine sections render; with the provider disabled the page shows 'No answer was generated' with the provider's own explanation and still lists the seven retrieved concepts and their sources
- **F08** `browser journey: ask at intuitive depth, then press 'Formal depth'` → the new interpretation reports formal depth, and the question and context fields are unchanged
- **F09** `browser states across fixture and disabled providers plus an unavailable API` → each state renders its own message; the fabricated-citation case additionally says the answer was discarded rather than shown
- **Phase F** `npm run typecheck && npm run lint && npm run format:check && npm run build --workspace @navigator/web && Playwright journeys and screenshot sweep` → all clean; all journeys passed; no console errors and no horizontal overflow at 1280x900 or 360x780 in light or dark

## Blockers
_none_

## Important decisions
- Host toolchain: Node v22.14.0 via nvm at /Users/nick/.nvm/versions/node/v22.14.0/bin (the host default node is v26; every build command exports that nvm bin directory first so the engines range >=22 <23 is genuinely honoured). npm 10.9.2, Docker 28.5.2 + Compose v2.40.3, sqlite3 3.51.0, GNU Make 3.81, python3 3.14.6.
- Host Ollama confirmed at http://127.0.0.1:11434 and lists qwen3.8:27b-mlx (18.2 GB, nvfp4, capabilities completion/vision/tools/thinking). No model pull is needed for the ordinary stack.
- Git identity is configured (Nicholas F), so one local commit is made per completed phase. Nothing is ever pushed or published.
- .npmrc sets save-exact=true (runbook B00 requires exact versions) and engine-strict=true so the frozen Node 22 LTS engines range is enforced rather than warned about.
- TypeScript pinned to 6.0.3, not the 7.0.2 'latest' tag: typescript-eslint 8.70.0 (the only current release line) declares peer typescript >=4.8.4 <6.1.0, so TypeScript 7 would leave the project without type-aware linting. 6.0.3 is a current stable release on the build date and is the newest version the whole toolchain supports.
- katex pinned to 0.16.47, not the 0.18.7 'latest' tag: rehype-katex 7.0.1 (latest) declares a katex ^0.16.0 dependency. 0.16.47 is the newest release inside the range the current rehype-katex supports.
- Zod 4 supplies z.toJSONSchema() natively, so no separate zod-to-json-schema dependency is used for C01. Frontmatter is parsed with the yaml package plus an explicit delimiter split rather than gray-matter, so Markdown is never handed to a templating engine.
- The docs plugin is mounted at routeBasePath '/' rather than 'concepts'. Docusaurus resolves frontmatter 'slug' relative to routeBasePath, so the frozen canonical slug '/concepts/<name>' produced '/concepts/concepts/<name>' under routeBasePath 'concepts'. Mounting at the site root reproduces the frozen slugs exactly while src/pages still owns /, /search, /explore, /ask and /about (verified: each route renders its own h1).
- Two enumerations are not spelled out in the runbook and were derived rather than invented freely. sources[].supports is the kebab-cased set of the ten substantive Tier 1 section headings from §4.1 (the example value 'uses-and-applicability' matches '## Uses and applicability' exactly); 'sources' and 'prerequisites-and-next-connections' are excluded because they are navigational rather than claims. sources[].source_kind extends the example value 'authoritative-secondary' with primary-research, preprint, reference-documentation, lecture-or-course, implementation and dataset-or-benchmark.
- Category paths must begin with one of the three atlas areas named in the product brief — Artificial Intelligence, Mathematics, Programming — so that F04's 'three top-level atlas categories' is a validated fact rather than a convention.
- Raw HTML in canonical Markdown is captured by the loader as data and rejected by validation. Docusaurus parses canonical .md as CommonMark, which would otherwise pass raw HTML straight through to the reader.
- URL reachability at 2026-09-16: 18 of 19 source URLs returned HTTP 200. https://ieeexplore.ieee.org/document/726791 (the LeNet-5 paper) returns HTTP 202 rather than 200 — a response, not a failure, but the page body was not fetched, so reachability there is weaker evidence than for the others. Reachability was never treated as evidence that a source supports a claim; each sources[].supports list names only sections the source materially supports.
- Page length: the eleven Tier 1 pages run 982-1235 words against the runbook's 'Aim for 400-800 useful words'. The runbook subordinates length to correctness in the same sentence ('correctness is more important than length'), and the twelve-section Tier 1 template leaves roughly 80-100 words per section at this length. The overrun is recorded as a deliberate, measured deviation rather than trimmed by cutting qualifications, derivations or worked arithmetic.
- Runbook checks D06 and F04 name 'gradient descent' as the multi-category acceptance target, but gradient descent is not one of the eleven concepts C05 freezes, and C05 forbids any twelfth concept. Those checks are run instead against concepts that do occur in several categories — resnet and vgg (Computer Vision primary, Architectures secondary), receptive-field and lenet (Architectures primary, Computer Vision secondary) and translation-equivariance (Mathematics/Analysis primary, Architectures secondary). The property being checked is unchanged: one canonical doc plus at least one reference entry, reachable through each assigned category, resolving to one URL.
- Eleven confirmed technical errors found by adversarial review were corrected in the slice, each re-derived independently before the fix. (1-3) cross-correlation.md: under the page's own convention the reflection falls on the FIRST argument, so the identity is f*g = f-tilde conv g, not f conv g-tilde; the worked example was relabelled so the kernel occupies the template slot, its displacement ranges stated explicitly, and the symmetric-kernel case rebuilt with a kernel symmetric about the origin ([1,2,1] at -1..1) because [1,1] at 0..1 is symmetric about 0.5 and the two operations then agree only up to a shift. (4) convolutional-layer.md no longer calls the 1998 paper the original formulation of the trainable layer; the 1989 priority is stated and explicitly marked as unsupported by any source cited on the page. (5) pooling.md: local averaging with subsampling is the neocognitron's C-layer, not the S-layer. (6) backpropagation-through-convolution.md summary said 'convolution with the flipped kernel', which is the double flip the page itself warns against. (7-9) vgg.md: channels double at the first three pooling stages not four; activation volume falls by half only while channels double and by a quarter afterwards; compute is spread almost evenly per layer across blocks 1-4 with blocks 3 and 4 largest at 29.9% each, not concentrated in block 1, which is the second cheapest. (10-11) resnet.md: 3.57% top-5 is the winning six-model ensemble, not a single ResNet-152 (4.49%); and ResNet-152's lower arithmetic cost comes from its aggressive stem and bottleneck trunk, since VGG's dense head is under 1% of its multiply-accumulates.
- The 1989 paper 'Backpropagation Applied to Handwritten Zip Code Recognition' was NOT added as a source: the MIT Press page returns HTTP 403 to a bounded request, the IEEE mirror returns 202 without a body, and the only 200 response is an http-only PDF whose contents could not be verified here. Rather than cite an unverified URL, the claim is stated in prose and explicitly marked unsupported, as AGENT_CONTENT_CONTRACT.md requires.
- A ranking bug was found by the D04 tests and fixed: the final sort broke ties by title, which discarded BM25 ordering and put 'Pooling' above 'Translation Equivariance' for 'translation invariance'. Bands are now sorted by band only, with insertion order as the tie-break, because each band is already internally ordered. Separately, 'matched filter' is a genuine BM25 tie between cross-correlation and convolutional-layer (-1.355 vs -1.353, under 0.2%); that test asserts the result set rather than an arbitrary winner, and a discriminating phrase (Wiener-Khinchin) covers the single-winner case.
- Retrieval needed a second FTS match mode. searchConcepts ANDs every term, which is right for a search box but means a whole-sentence question matches nothing — including the runbook's own G07 acceptance question. A 'any' mode was added that ORs the terms and lets BM25 rank; retrieval uses it, /api/search keeps 'all'.
- The Docusaurus @site/* path alias had to be restated in apps/web/tsconfig.json. A relative 'paths' entry resolves against the config file that declares it, and @docusaurus/tsconfig declares it inside node_modules, so @site/* pointed into the vendored package rather than at the workspace.

## Next action
Begin G00 — build the API image.

# Knowledge Navigator Version 2 Build State

Resume point for any agent continuing the Version 2 build.

**Build authority:**
[`KNOWLEDGE_NAVIGATOR_V2_BUILD_RUNBOOK.md`](./KNOWLEDGE_NAVIGATOR_V2_BUILD_RUNBOOK.md).
That file — not this one — holds the v2 product contracts (§4), the frozen
decisions (§3), every v2 checkpoint, and the v2 Definition of Done (§17). Frozen
decisions are deliberately **not** copied here so there is one authority and no
drift.

**Version 1 evidence lives in [`BUILD_STATE.md`](./BUILD_STATE.md) and stays
there.** It must never be copied wholesale into this file. V1 is a finished
dependency: read it to learn what already works and why a decision was taken,
and record here only what Version 2 adds, changes, or re-verifies.

**Project root:** `/Users/nick/Projects/knowledge-navigator` (the folder holding
both runbooks). The build never reads or writes parent directories.

**How to resume:** read the v2 runbook in full, then the v1 runbook,
`BUILD_STATE.md`, `README.md` and `AGENT_CONTENT_CONTRACT.md`, then this file.
Start at the first checkpoint not listed under _Completed checkpoints_,
honouring its **Depends on** line.

## Current checkpoint

S00

## Completed checkpoints

- **J00** — Inventory. Project root `/Users/nick/Projects/knowledge-navigator`, branch `main`, HEAD `0b454cf` ("Definition of Done: all 33 statements verified"). `BUILD_STATE.md` reads `COMPLETE`. Working tree held exactly one untracked file, `KNOWLEDGE_NAVIGATOR_V2_BUILD_RUNBOOK.md`, which the checkpoint permits. Toolchain: Node v22.14.0 (nvm), npm 10.9.2, Docker 28.5.2 with Compose v2.40.3, sqlite3 3.51.0, GNU Make 3.81, Python 3.14.6. The v1 Compose stack was running and healthy (api and web; only web published, on 127.0.0.1:3000). No unrelated user files are at risk and nothing outside this folder is read or written.
- **J01** — `V2_BUILD_STATE.md` created with the six required sections plus the pointer back to `BUILD_STATE.md` for v1 evidence.
- **J01** — V2_BUILD_STATE.md created with the six required sections, a pointer to the v2 runbook as authority, and an explicit statement that v1 evidence stays in BUILD_STATE.md and is never copied here.
- **J02** — V1 regression baseline captured before any v2 change. make check 9/9 stages in 12 s with 330 unit tests across 16 files; 7 browser journeys passed in 4.9 s; smoke test 16 checks passed with the stack torn down and the named volume preserved. Corpus hash bb96930aab75dd2c3741c91919f65b8ba3ea53b5a7c13cea1588c7fd1edff96e over 11 Tier 1 pages, 96 internal links, 19 distinct sources, 40 citations, 0 errors. The eleven per-file SHA-256 hashes are pinned below for K08 to compare against.
- **J03** — Version 2 local-data boundaries added to .gitignore, .dockerignore and .prettierignore. Git and the Docker context now exclude .navigator/ (proposal workspaces and private exports), data/personal.db and its WAL/SHM siblings, data/uploads|artifacts|extraction/, *.upload.tmp, *.extract.tmp, exports/ and *.navigator-export.json. The Docker context additionally excludes the v2 runbook and V2_BUILD_STATE.md, matching how the v1 pair is treated. content/atlas.yaml and content/graph-only/ are deliberately NOT ignored by either boundary, because the API image compiles them.
- **J04** — content/graph-only/ created with a .gitkeep and staged. No Tier 2 or Tier 3 content invented.
- **J05** — Phase J committed as 1ab96ce. Staged diff inspected: seven files, none of them a database, an environment file, a private export or a proposal workspace; every credential-pattern hit in the diff was the v2 runbook prose describing the checks that look for credentials. Working tree clean.
- **K00** — Atlas schema implemented in packages/core/src/atlas.ts against v2 runbook section 4.1: Zod strictObject schemas for the document, areas, categories and candidates, plus validateAtlasStructure for every rule Zod cannot express. A candidate has no field for a factual summary, and writing one is refused by name. Diagnostic and sortDiagnostics moved to a new packages/core/src/diagnostics.ts so the atlas, graph-only and corpus validators can share them without an import cycle; validate.ts re-exports both so no existing caller changed. ATLAS_STATUS_TRANSITIONS makes the editorial state machine explicit: a covered candidate may only fall back to candidate, never be deferred or re-proposed.
- **K01** — content/atlas.yaml seeded from Appendix A: 3 areas, 38 categories, 291 candidates, 11 covered, 280 uncovered, 8 deliberately empty categories. Appendix A names 331 labels of which 329 are distinct after normalisation; the two repeats, Lean and Logic Programming, each became ONE candidate filed in both of its categories, exactly as the contract requires. No candidate carries a summary, a source or a relationship.
- **K02** — Graph-only Tier 3 identities implemented in packages/core/src/graph-only.ts. The schema IS conceptFrontmatterSchema narrowed to tier 3, so every identity and relationship rule is shared rather than copied; the module adds only the storage contract — one YAML document, no Markdown body, file name derived from the slug. LoadedConcept gained a format discriminator (markdown | graph-only) so corpus validation and compilation handle both formats through one path.
- **K03** — conceptKinds extended with paper, person, historical-event, dataset and benchmark. The twelve v1 kinds keep their spelling and their order, so no existing page changed. No instance of an added kind was created.
- **K04** — unresolved_references added to the shared frontmatter schema, so Tier 1, Tier 2 and Tier 3 all carry it. Labels are normalised with the same function used for concept names; duplicates within one identity are rejected, as is a label that is the identity own title or alias. Sections are validated against the template vocabulary and must be empty on a graph-only identity, which has no sections. Corpus rules add two more: a proposed category must resolve in the atlas, and a label the corpus now answers to is reported as stale rather than left in the backlog. unresolvedReferenceId() and backlogGroupId() give each entry a deterministic id and each label one group.
- **K05** — claims and evidence implemented per section 4.4. Claim status is supported | conditional | disputed | unsupported. Evidence carries a human-readable locator and an optional note, and its source_id must appear in that identity own sources list. Claim ids are unique within a page and across the corpus. Status and evidence must agree: supported, conditional and disputed each need at least one piece of evidence, and unsupported must have none. Section splitting moved into packages/core/src/sections.ts (with TIER_1_HEADINGS extracted to headings.ts) so the promotion rule can ask which sections actually have prose.
- **K06** — loadCorpus now loads Markdown pages, graph-only YAML identities and the atlas together and validates them in one pass. File ordering is by name within each format and by concept id in the result. CorpusResult gained atlas, atlasHash and a CoverageSummary, all reported separately from the canonical identities so a candidate is never mistaken for a concept. New corpus rules: a covered candidate must name a concept that exists, at most one candidate may cover a concept, and a candidate whose label already names a concept must be marked covered. A missing graph-only directory or atlas file is not an error, and atlas-dependent rules are skipped when no atlas is supplied.
- **K07** — Three JSON Schemas are now generated from the same Zod definitions the runtime uses: schemas/concept.schema.json, schemas/graph-only.schema.json and schemas/atlas.schema.json. navigator schema writes all three and --out now names a directory. AGENT_CONTENT_CONTRACT.md was rewritten for the v2 model: canonical versus candidate, the four coverage depths, the five added kinds, the graph-only file shape, unresolved references, claims and evidence, the shared section vocabulary, and four new checklist lines.
- **K08** — V1 compatibility proven. Validation reports 11 concepts, all Tier 1, all generated-draft, all markdown format, zero Tier 2, zero Tier 3, zero errors, corpus hash bb96930a unchanged from the J02 baseline. All eleven canonical files are byte-identical to their J02 hashes, and git shows no modification under content/concepts.
- **K09** — Phase K committed as cd0f2eb. make check passed all 9 stages in 11 s with 499 unit tests across 23 files. Staged diff reviewed: 29 files, no database, no environment file, no private data; the only credential-pattern hit was the word "Tokenization" in an atlas candidate label. Working tree clean.
- **L00** — SCHEMA_VERSION incremented to 2. Ten new tables added — atlas_areas, atlas_categories, atlas_candidates, atlas_candidate_aliases, atlas_candidate_categories, unresolved_references, unresolved_reference_sections, unresolved_reference_categories, claims, claim_evidence — plus content_format and has_article columns on concepts. Every v1 table and column is unchanged, and the API still refuses a database whose schema version is newer than the one it understands.
- **L01** — The whole atlas compiles: 3 areas, 38 categories (8 with neither a candidate nor a child), 291 candidates and 293 candidate-category links, sorted by depth then id so a parent row always exists before its child. A table CHECK enforces that a covered candidate names exactly one concept and no other status names any, and a partial unique index stops two candidates claiming the same concept.
- **L02** — Graph-only identities compile into concepts, aliases, categories and relationships with content_format = graph-only, has_article = 0, an empty body and a source_path under content/graph-only/. They are findable by title, alias and summary in FTS but contribute no prose, so a Tier 3 identity can be searched as metadata and can never look like an article nobody wrote. Verification fails the build if has_article ever disagrees with format and tier.
- **L03** — The editorial backlog compiles into unresolved_references plus its section and category tables, each row carrying a deterministic id from its source concept and label, and a group_id from the normalised label alone. listBacklog() groups by group_id, keeps every source record, marks a group blocking when any one page does, and sorts blocking first, then by how many pages are waiting, then by label.
- **L04** — Claims and evidence compile into claims and claim_evidence, with claim_evidence.source_id a foreign key into sources so a locator can never point at a work the corpus does not carry. getConceptEvidence() returns claims in declaration order with their evidence, plus the page sources annotated with how many claims each supports, and a hasClaimMapping flag so a page with no claims says so rather than looking unsupported.
- **L05** — generated/graph.json extended: every node carries format, hasArticle, candidateId, unresolvedReferences and claims; the document carries atlasHash and a coverage block of aggregate counts. The coverage block is counts only — no candidate label, no backlog text, no evidence note — because graph.json is built into the web image and served to anyone who can reach the site.
- **L06** — Four CLI commands added: navigator coverage summary, coverage candidates [--area|--category|--status], coverage unresolved [--blocking] and evidence <concept-id-or-slug>. All accept --json for stable machine output and --limit/--offset for paging; the default output is readable text.
- **L07** — Atomic and deterministic compilation preserved under the v2 model. Pre-rename verification now also checks that has_article agrees with format and tier, that every covered candidate names a compiled concept, and that every atlas category reaches a root area that exists.
- **L08** — Phase L committed as aa38899. make check passed all 9 stages in 13 s with 552 unit tests across 25 files; the 7 v1 browser journeys still pass. Working tree clean.
- **M00** — Five read-only endpoints added in apps/api/src/routes/coverage.ts: GET /api/coverage/summary, /atlas, /candidates, /unresolved and /api/evidence/:conceptId. Filters are validated with strict Zod objects so an unknown query parameter is rejected rather than ignored; lists are capped at 500 with limit/offset and every response carries total and truncated, because a cap that silently truncates is a lie.
- **M01** — The /coverage page shows all three areas with counts for Tier 1, Tier 2, Tier 3, candidates, atlas categories, unresolved references and claims, plus review-state and candidate-status breakdowns. It explains in plain words that a candidate is a label with no definition, no sources and no relationships and is never used to answer a question. When the API is unavailable it says so and falls back to the counts compiled into the build.
- **M02** — The atlas outline renders every area, every category (nested) and every candidate. Categories are disclosures closed by default with their own counts on the summary, because 291 candidates in one flat list is a wall nobody reads; filters for coverage state and review state open everything and omit branches that match nothing. A candidate opens an inline editorial detail panel — never a URL — stating that it is a lead, not knowledge, and showing only its categories, its aliases, its note and its id.
- **M03** — The /backlog page has two separate views — unresolved references and atlas candidates — because they come from different places and mean different things. Unresolved items are sorted blocking first, then by how many pages are waiting, then by label, and each keeps every source page and reason. The page offers filters only; it contains no form and no mutation.
- **M04** — Identity panels at /identity/<key>, one static page per canonical concept, showing summary, concept id, address, kind, tier, review state, storage format, aliases, categories, the covering candidate, relationships in both directions and sources. A Tier 3 identity gets a prominent "No article yet" notice explaining that it is a stable address with nothing written about it; a concept that does have an article links straight to it.
- **M05** — Concept pages show claim-level evidence: status, statement, section, source, locator and note, with the four statuses distinguished by colour, border texture AND wording so none of them can be mistaken for another. A page with no claims says plainly that no claim-level mapping exists and that this is not evidence the page is unsupported. Unresolved references declared by a page are listed in their own section as ideas named but not explained here.
- **M06** — Coverage added to the main navigation between Ask and About. Backlog is deliberately not a top-level item and is reached from Coverage, which is where a reader learns what a gap means. Home, Explore, Search, Ask and About are unchanged.
- **M07** — Phase M committed as 64c865e. make check 9/9 stages in 14 s with 545 unit tests; 32 browser journeys passed; screenshots reviewed at 1280x900 light and dark and 360x780, zero console errors and zero horizontal overflow on every new page.
- **N00** — apps/api/src/personal/ added with an opener for PERSONAL_DATABASE_PATH, defaulting to /private/personal.db in containers and configurable to data/personal.db on a host. WAL, foreign keys and a 5 s busy timeout are set before any migration runs, and the schema version lives in PRAGMA user_version. openPersonal() wraps it in a handle like the canonical index handle: opening never throws, and a route that would write answers 503 with a reason rather than silently succeeding.
- **N01** — Personal schema version 1: projects, research_sessions, notes, concept_familiarity, saved_items, artifacts and export_history. Every record carries created_at and updated_at as UTC ISO 8601, everything archivable carries archived_at instead of being deleted, foreign keys cascade from a project, and useful indexes cover the archived, project and concept lookups. Each migration runs in its own transaction together with its version bump.
- **N02** — Project storage: create, list, get, rename, describe, archive and restore. Title capped at 200 characters and description at 8,000. Archived projects are excluded from the default listing and recoverable. projectContents() counts sessions, notes, saved items and artifacts for the overview.
- **N03** — Research sessions belong to one project and carry a title, an optional starting question, an optional context summary and timestamps. Creation is explicit: there is no code path from an assistant request to a session row.
- **N04** — Notes may link to a project, a session, a canonical concept, a saved item or an artifact. The body is Markdown SOURCE, stored and returned byte for byte — nothing strips it and nothing renders it, so a note containing <script> stays a note containing those characters and the browser escapes it on the way out. 64 KiB cap, edit, archive and restore.
- **N05** — Familiarity stores at most one record per canonical concept with the four levels from section 4.6 and an optional 1,000-character note. The only writer is an explicit request. setFamiliarity also refuses a concept the compiled index does not carry, so a level can never distort a path by naming something that does not exist.
- **N06** — Saved items support concept, source, assistant-answer, comparison, path and next-check, each with its own Zod payload schema and a payload_version. A saved assistant answer keeps the model, mode, depth, timestamp, resolved citations and the exact structured result the API returned. Concepts, sources, comparisons and paths deduplicate on a derived key inside a project; answers and next-checks do not, because each is a distinct event.
- **N07** — Versioned JSON endpoints under /api/personal/ for status, projects, sessions, notes, saved items and familiarity. POST creates, PATCH updates, and archive/restore are POST actions — there is no DELETE anywhere in the product. Every child route resolves through the project, so an id from one project cannot be reached through another. The personal-data volume was added to Compose and the Dockerfile at the same time so the container stays usable at this phase boundary; S00 re-verifies it.
- **N08** — Log redaction extended from two fields to eighteen, covering project titles and descriptions, note bodies, session questions and context, saved labels, statements, rationales and payloads, familiarity notes, artifact names and extracted text, and answer and interpretation text. Each is redacted at the top level of a log object and under req.body and res.body.
- **N09** — Phase N committed as d9e468a. make check 9/9 in 14 s with 627 unit tests across 28 files; 32 browser journeys passed; make security-review 23 checks, 22 pass / 0 fail / 1 documented note. data/personal.db and data/personal.db-* are ignored by Git and no database file is staged.
- **O00** — /workspace added with a Workspace link in the main navigation. Its opening section states where the data lives — locally, in its own database only this service opens, not in Git, not in an image, and not recoverable from anywhere else — and points at the export. The empty state explains what a project is before there is one.
- **O01** — Project create, choose, rename, archive and restore. Archive asks for confirmation and says in the prompt that nothing is deleted; restore needs none. The chosen project is remembered in localStorage and carried in ?project= so a project view is linkable.
- **O02** — The project view has Overview, Sessions, Notes, Saved, Artifacts and Export as buttons over one route — headings and state, not a router. Overview carries the description, four counts, the created date, the id, the rename form and the archive action.
- **O03** — Concept pages and identity panels carry a private layer: add a note, see the notes already on this concept, and the plain statement that nothing there changes the page above. Notes render as text with white-space preserved, never as markup.
- **O04** — Familiarity controls on concept pages and identity panels: Not set plus the four levels, each with its meaning shown, changing or clearing in one click. Familiarity is per concept rather than per project, because it describes the person rather than a piece of work.
- **O05** — Save to project on concept pages, showing what will be saved and to which project before the action. The button reads Saved to project once the concept is already in the project, so a second save is visibly a no-op.
- **O06** — Save result to project on the Ask page. It keeps the structured result, citations, mode, depth, provider, model and time, and the question. The research context is a separate opt-in tick box, because context describes unpublished work and is the most sensitive thing typed here. An answer can be filed under a session.
- **O07** — Sessions are created from the project view and are offered on the Ask save panel. Reopening a project shows its sessions in the same order.
- **O08** — Phase O committed as dff35b5. make check 9/9 in 14 s with 627 unit tests; 46 browser journeys passed in 16 s; security review 22 pass / 0 fail / 1 note; screenshots at 1280x900 show zero console errors and zero horizontal overflow on the workspace and on a concept page with the private layer.
- **P00** — Ingestion dependencies pinned at exactly @fastify/multipart 10.1.1 and pdfjs-dist 6.3.289 in the API workspace. No OCR, image, office-document or general parser framework was added.
- **P01** — Artifact records store the extracted text plus label, original file name, media type, byte count, character count, SHA-256 and extraction warnings. The original bytes are never retained — there is no column for them. The same bytes in one project are one artifact, matched on the hash; archive and restore replace deletion; selectArtifactsForContext returns only ids the caller named, from its own project, excluding archived ones.
- **P02** — Text and code extraction decodes UTF-8 with replacement, normalises newlines, strips a BOM, and refuses binary content, an unsupported extension, an empty file, an over-size file and a whitespace-only file. Nothing is executed: a .py file is decoded and stored as text.
- **P03** — Notebook extraction keeps Markdown and code-cell source in cell order with a heading naming the cell type and index, and drops outputs, attachments, execution counts, widget state and every metadata field but the language name.
- **P04** — PDF extraction runs pdfjs-dist in the API process with no worker, no eval, no font loading, no system fonts and no XFA. Pages are joined with explicit [page N] markers, the page limit is 300, and a scanned PDF is reported as no_extractable_text rather than stored as an empty artifact — this product has no OCR and does not pretend otherwise.
- **P05** — Upload endpoints under one project: POST multipart with a 10 MiB hard limit and one file, plus list, detail, archive and restore. The file name decides how the file is read, never the browser Content-Type; a .pdf sent with a disagreeing media type is refused without being read; and the extraction failure codes map to 422 with the reason.
- **P06** — The project Artifacts section uploads one chosen file at a time, shows extraction progress, metadata, warnings, a safe plain-text preview, archive and restore. It states before any file is chosen that the text is kept, the file is not stored, nothing is executed and there is no OCR.
- **P07** — Ask may select a project and up to five artifacts or notes. The picker shows each item character count and the running total, nothing is ticked by default, and only ticked ids are sent. Private material has its own 12,000-character budget, is taken in the order the researcher listed, and truncation is reported per item and in total.
- **P08** — Assistant results carry a privateContext block of ids, labels and counts — never a word of the material — rendered in its own Private context used section with a dashed rule that borrows none of the evidence palette. The system prompt states that private material is data, may be referred to in prose, and may NOT be cited, and it never enters the citable-id list.
- **P09** — Phase P committed as 2820047. make check 9/9 in 15 s with 686 unit tests across 31 files; 54 browser journeys passed; security review 21 pass / 0 fail / 2 documented notes; a busybox build against the real Docker context confirms no .navigator, personal database, upload, artifact or extraction path can reach an image.
- **Q00** — Deterministic comparison parser: packages/core/src/compare.ts extracts the five section rows plus summary from stored canonical Markdown, preserving Markdown source and distinguishing empty, no-section and no-article.
- **Q01** — compareConcepts() and POST /api/compare: ids validated, duplicates collapsed, two to four enforced, caller order preserved, with review states, relationships, sources and claim coverage in the payload.
- **Q02** — The /compare page: concept search shared with Path, two-to-four selection carried in the URL, a grid that is columnar at tool width and stacked at 360px, missing markers with a stated reason, review-state rails per column, source links, and an optional project save.
- **Q03** — Explain this comparison: POST /api/compare/explain sends the rendered table as the whole context, citable ids are the compared concepts and their sources, and a rejected synthesis leaves the table untouched.
- **Q04** — Prerequisite traversal in packages/core/src/learning-paths.ts: only requires and prerequisite_of order anything, both directions normalise to before-to-after keeping which page declared it, backwards breadth-first with deepest-first reading order, ties broken on concept id, cycles visited once and reported.
- **Q05** — Familiarity applied transparently: strong becomes a starting point, working stays in the route marked likely known, unfamiliar and recognize change nothing, include puts a skipped concept back, and every record that changed the result is named in familiarityEffects.
- **Q06** — POST /api/paths and the /path page: target search, optional project for familiarity, ordered steps numbered in reading order, the declared edge behind every step, familiarity effects named, missing graph information, canonical links, and no invented edge anywhere.
- **Q07** — Comparisons and paths are saved whole and exported as Markdown. The saved shapes live in packages/core/src/export.ts so the private store and the exporter share one definition; navigator export list and navigator export saved <id> read the private database read-only and write one file to .navigator/exports with canonical page URLs and source URLs.
- **Q08** — Phase Q committed as fc7f48a with a clean working tree.
- **R00** — The proposal bundle contract in packages/core/src/proposal.ts: manifest schema, the five bundle files, six statuses with a forward-only transition table, allowed paths restricted to content/concepts/*.md and content/graph-only/*.yaml, tier and file kind pinned to each other, and a base-commit presence check. Published as schemas/proposal.schema.json.
- **R01** — navigator proposal prepare <backlog-id> --tier 2|3 writes manifest.json and REQUEST.md into .navigator/proposals/<id>/. The brief is derived from the compiled index: the one file that may be written, the exact identifiers it must carry, the pages waiting on it, the tier's content rules, the checks to run, and where to stop. No model is invoked.
- **R02** — navigator proposal validate <dir> checks seven things against a throwaway git index: the patch applies to the recorded base, touches only allowed paths, adds no secret or ignored artefact, keeps concept_id and slug, raises no review state, still passes content validation, and carries a reviewer summary. The verdict is written to validation.json and nothing is applied.
- **R03** — navigator proposal accept <id> --confirm <id> [--dry-run] and navigator proposal reject <id> --confirm <id> --reason <text>. Acceptance requires a matching confirmation, a proposal in review, a clean tree, HEAD at the recorded base, a branch that does not exist, and validation passing now; it then creates content/<id>, applies and stages the patch, runs content validation and a compile against the result, and commits nothing.
- **R04** — HERMES_CONTENT_PROFILE.md: one proposal, one target, allowed paths only, an isolated worktree, generated-draft always, sources that were read and uncertainty stated, no merge, push, publish, tag, deploy or pull request, the three exact checks, RESULT.md with what could not be checked, the change left uncommitted, and a handoff that ends at review.
- **R05** — scripts/hermes-content-task.mjs --check locates the hermes binary (HERMES_BIN or PATH), reports its version, confirms the top-level help mentions project and kanban, and confirms hermes kanban create offers --workspace, --project, --max-runtime, --max-retries and --completion-contract. It never installs, updates or configures anything.
- **R06** — The dry run is the default: --proposal <id> prints the shell-escaped hermes kanban create it would run, with project and board slug knowledge-navigator, the repository as the workspace, a two-hour runtime cap, three retries, and a completion contract that ends the task at review. --local-only is added only when the installed build offers it; otherwise the contract and the body say the task is local-only. One-time project and board setup commands are printed when either is absent, never run.
- **R07** — --execute --confirm <proposal-id> dispatches through spawn with an argument array and no shell. It requires a matching confirmation, a proposal still in prepared, a clean repository, a Hermes that has every required flag, and an existing project and board; it records the returned task id, moves the proposal to running, and never creates Hermes configuration.
- **R08** — navigator proposal import-hermes <id> --worktree <path> confirms the worktree shares this repository's common git directory, requires it to still be at the recorded base with the change uncommitted, turns the worktree into a patch through a throwaway index, copies RESULT.md into the bundle, runs validation, writes validation.json, and sets the status to review. Nothing is merged or accepted.
- **R09** — README section 'Proposing content with an agent': prepare, read the brief, one-time Hermes setup run by the person, dry run, dispatch with a confirmation, inspect the task, import, validate, read, then accept or reject. It states that the Hermes gateway warning after an update is the reader's to act on and that neither the product nor its tests restarts anything. Added an npm run navigator script so the documented commands are runnable as written.
- **R10** — Phase R committed as 0402cb4 with a clean working tree.

## Last successful checks

- **J00** `pwd && git rev-parse HEAD && git status --short && docker compose ps -a` → root correct; HEAD `0b454cf`; only the v2 runbook untracked; api and web both healthy.
- **J01** `read V2_BUILD_STATE.md as a fresh agent would` → Current checkpoint, Completed checkpoints and Next action together identify J02 without reference to anything but the two runbooks.
- **J02** `make check` → all 9 stages passed in 12 s; 330 tests in 16 files; graph.json and sidebars.generated.ts byte-identical across two compiles
- **J02** `npm run test:browser` → 7 journeys passed in 4.9 s
- **J02** `./scripts/smoke.sh` → 16 checks passed; containers torn down, navigator-data volume preserved
- **J02** `shasum -a 256 content/concepts/*.md` → digest of the eleven hashes: 3825f5e70952be855003b3f6d0afc8af5cf3d4c144c1955538090d12692041df (list recorded under Important decisions)
- **J03** `git check-ignore over 15 private paths and 11 canonical v2 paths` → all 15 private paths ignored (proposal manifest, private export, personal.db and -wal/-shm, uploads, artifacts, extraction temporaries, external export, .env, knowledge.db, node_modules); all 11 canonical paths trackable (content/atlas.yaml, content/graph-only/*.yaml, concept Markdown, the six new core modules, the personal API module, the new web pages, the Hermes script, .env.example, package-lock.json, generated/graph.json, sidebars.generated.ts)
- **J03** `docker build against a busybox stage that asserts on the real build context` → exit 0 — .navigator/, data/personal.db, both v2 authority documents, .git, .env, data/ and node_modules are absent from the context, while content/concepts/resnet.md, package.json and AGENT_CONTENT_CONTRACT.md are present. A real .navigator/proposals/p1/manifest.json and data/personal.db existed on disk during the test and neither reached the context.
- **J04** `npm run validate` → 11 concepts, 11 Tier 1, 11 generated-draft, 4 categories, 20 relationships, 96 internal links, 19 sources, 40 citations, corpus hash bb96930a unchanged, 0 errors
- **J05** `git add -A && git diff --cached --name-only && credential scan && git commit && git status --short` → commit 1ab96ce; staged .dockerignore, .gitignore, .prettierignore, the v2 runbook, V2_BUILD_STATE.md, content/graph-only/.gitkeep and generated/graph.json; tree clean afterwards
- **K00** `npx vitest run packages/core/tests/atlas.test.ts` → 40 tests passed — one valid atlas plus one failing case for each rule (unknown key, factual summary, wrong schema_version, malformed id, bad status, no categories, malformed YAML, empty file, wrong area set, duplicate area/category/candidate id, id collisions, missing parent, self-parent, cycle, cross-root parent, duplicate sibling title, missing category, candidate on a root area, repeated category, normalised title and alias collisions, covered without a concept id, non-covered with one). A document breaking four rules at once returns all four in a stable order, identical across two runs.
- **K01** `npx vitest run packages/core/tests/atlas-seed.test.ts` → 9 tests passed — the shipped atlas is structurally valid; the three root areas are present; every Appendix A label is represented exactly once after normalisation (re-derived from the runbook by the test itself, independently of the generator); Programming has 5 categories and 49 candidates with zero canonical pages behind them; exactly the eleven existing concepts are covered and every other candidate is unmapped; empty categories survive; and all four category paths the canonical corpus already uses resolve.
- **K02** `npx vitest run packages/core/tests/graph-only.test.ts` → 21 tests passed — a valid identity loads with no body, headings or links and needs no Tier 1 template; a Markdown body after a separator, an explicit body key, any unknown key, an empty file and unparseable YAML are all rejected; tiers 1, 2 and 4 are rejected; the shared identity rules (dotted id, slug/id agreement, primary_category, self-relationship, atlas area) all fire on YAML; and in a mixed corpus a file-name mismatch, a duplicate concept_id, a duplicate slug, a normalised alias collision with a Markdown page and a dangling relationship target are all caught.
- **K02** `npx vitest run` → 400 tests passed across 19 files — the 330 v1 tests plus 70 new ones; no v1 test changed
- **K03** `npx vitest run packages/core/tests/schema.test.ts && npm run validate && diff of content hashes against the J02 baseline` → 49 schema tests passed, including one asserting the first twelve entries are still exactly the v1 list and one that parses a page for every one of the 17 kinds; validate still reports 11 concepts, 11 Tier 1, corpus hash bb96930a unchanged; all eleven per-file SHA-256 hashes identical to the J02 baseline
- **K04** `npx vitest run packages/core/tests/unresolved-references.test.ts` → 25 tests passed — a full entry parses, defaults fill in, the field defaults to empty, Markdown and YAML both accept it; missing/empty label or reason, unknown key, bad section name, repeated section, bad proposed_kind, non-atlas area and non-boolean blocking all fail with a field path; duplicate and self-referential labels fail; the atlas rule fires on an unknown category and is skipped when no atlas is supplied; a stale label is reported; ids are deterministic and always valid dotted segments
- **K05** `npx vitest run packages/core/tests/claims.test.ts` → 33 tests passed — all four statuses, evidence with and without a note, one source supporting several claims; malformed claim_id, section, statement, status, unknown key and missing locator; every status/evidence disagreement; evidence pointing at an uncited source; duplicate claim ids within a page and across two pages; and the promotion rule for all three promoted review states, including a promoted page that covers every section, a section with a heading but no prose, and a promoted Tier 2 page with no claims
- **K06** `npx vitest run packages/core/tests/mixed-corpus.test.ts` → 12 tests passed — a Tier 1 + Tier 2 + Tier 3 + atlas corpus validates cleanly, sorts by concept id across both formats, keeps the atlas out of the concept list, summarises coverage, is deterministic across two loads, includes graph-only files in the corpus hash, and works with no graph-only directory and no atlas; a single run reports a cross-format duplicate id, duplicate slug, normalised alias collision and two broken relationship targets together, and a malformed atlas does not swallow the concept diagnostics
- **K07** `npm run schema twice + npx vitest run packages/core/tests/contract-audit.test.ts` → both generations byte-identical (atlas 4c0b189c, concept 4fe7b293, graph-only 9d51980b); 25 audit tests passed — every concept kind, review state, claim status, section key, atlas status and Tier 1 heading (in order) appears in the written contract, as does every required field of the frontmatter, unresolved-reference, claim and candidate schemas; the files in schemas/ match the generator exactly; all three refuse unknown keys; and the seven v2 boundary statements are present
- **K08** `npm run validate && diff of shasum -a 256 content/concepts/*.md against the J02 baseline && git status --short content/` → 11 Tier 1, 0 Tier 2, 0 Tier 3, 0 errors; all eleven hashes identical; the only new path under content/ is the atlas file itself
- **K09** `make check && git add -A && git diff --cached review && git commit` → all 9 stages passed in 11 s; 499 tests; commit cd0f2eb; tree clean
- **L00** `npm run compile && sqlite3 data/knowledge.db build_meta, sqlite_master, PRAGMA foreign_key_check, PRAGMA integrity_check` → schema_version = 2; all 19 tables present; foreign_key_check empty; integrity_check ok
- **L01** `npx vitest run packages/core/tests/coverage.test.ts` → Programming and its category survive with zero canonical pages; an empty category survives; a sub-category nests under its parent rather than the area; all covered candidates resolve to distinct existing concepts; filters by area, category and status and pagination are stable
- **L02** `npx vitest run packages/core/tests/coverage.test.ts` → the Tier 3 fixture is a concept row with no article and an empty body, participates in a relationship from a Markdown page, appears in FTS with title/aliases/summary and an empty body, and is the only row with has_article = 0. The fixture lives in a temporary directory and is removed afterwards.
- **L03** `npx vitest run packages/core/tests/coverage.test.ts` → the same label written as "Strided convolution" and "strided-convolution" on two pages produces ONE group with two source records and both reasons intact; the group is blocking because one page says so; the proposed kind and categories survive and resolve to a real atlas category; and the backlog and the candidate list stay separate queries over separate tables
- **L04** `npx vitest run packages/core/tests/coverage.test.ts` → all four statuses compile; the unsupported claim is the only one with no evidence and the database is queried to prove it; no evidence row points at an absent source; one source supports four claims on one page and one on another; locator and note survive verbatim; a page with no claims reports hasClaimMapping false while keeping its sources
- **L05** `npx vitest run packages/core/tests/graph.test.ts` → 12 tests passed — hasArticle agrees with format and tier on every node, the covering candidate is named, aggregate counts agree with the nodes, and a search of the serialised file for fourteen private field names (personal, artifact, familiarity, saved_item, research_session, proposal, export_history, candidate_note, api_key, password, secret and others) returns nothing. The coverage block is asserted to contain numbers only.
- **L06** `npx vitest run packages/core/tests/cli.test.ts` → 23 tests passed, 12 of them new — summary in text and JSON, byte-identical across two runs; filters by area and by status; an empty result reported as an empty page and as plain text rather than an error; an unknown status rejected with exit 2 and the allowed list; deterministic ordering with non-overlapping pages; unknown subcommand rejected; evidence by id and by slug producing identical output; evidence JSON stable; unknown concept exits 1; a missing argument exits 2
- **L07** `npx vitest run packages/core/tests/compile-atomic-v2.test.ts` → 9 tests passed — two builds of identical input with SOURCE_DATE_EPOCH produce byte-identical database, graph JSON and sidebars; an atlas-only edit changes atlasHash and not corpusHash; and five deliberately broken v2 fixtures (atlas covering a missing concept, a graph-only file at the wrong tier, a backlog item proposing a category outside the atlas, a claim citing an uncited source, and two pages sharing a claim id) each fail the build while leaving all three prior outputs byte-identical, leave no .db.tmp behind, and recover exactly when undone
- **L08** `make check && npm run test:browser && git commit` → all 9 stages passed; 552 tests; 7 browser journeys passed in 4.6 s; commit aa38899; tree clean
- **M00** `npx vitest run apps/api/tests/coverage.test.ts` → 18 tests passed — healthy, empty, filtered by status/area/category, paginated without overlap and byte-identical on repeat, six invalid queries plus an unknown filter each rejected with 400 and a field-addressed issue list, 404 for an unknown concept, 400 for a malformed id, and 503 with a reason from all five routes when the index is unavailable. Two further tests prove no coverage read alters the database (SHA-256 before and after) and that POST/PUT/PATCH/DELETE on a coverage route are 404.
- **M01** `npx playwright test tests/browser/coverage.spec.ts` → Programming renders with 49 candidates and zero covered, and its count line says so; the tallies read 11 Tier 1 and 0 Tier 3; every area section is aria-labelledby its own heading; and the page has zero horizontal overflow at 360x780
- **M02** `npx playwright test tests/browser/coverage.spec.ts` → every category and candidate is reachable by keyboard (disclosure summaries and buttons); an empty category stays visible with "nothing here yet"; an uncovered candidate has no link at all and opens a panel saying it can never be used to answer a question; a covered one opens its concept; the covered filter narrows to exactly 11 entries and omits non-matching branches; the review-state filter narrows to 11; returning to Everything restores the empty categories
- **M03** `npx playwright test tests/browser/coverage.spec.ts` → reached from Coverage by link; both views switch; the empty backlog says "No page is waiting on a missing concept" rather than showing an empty list; a candidate item names its categories and its stable id; the page contains no form element. The two-sources-one-group behaviour is covered by the seeded fixture in packages/core/tests/coverage.test.ts.
- **M04** `npx playwright test tests/browser/coverage.spec.ts` → all three sampled identity URLs are real static HTML served with 200; an unknown key is a 404 with no route at all; the page renders with zero console errors and no hydration mismatch; every identity field is present; and no identity link appears in the Docusaurus sidebar
- **M05** `npx playwright test tests/browser/coverage.spec.ts` → the eleven v1 pages carry no claims, and the section says so in those words while keeping the existing source display intact. Supported, conditional, disputed and unsupported rendering is pinned by the CSS and by the seeded compile fixture; the frontmatter reader drops any claim whose status is outside the four.
- **M06** `npx playwright test tests/browser/coverage.spec.ts` → all five navigation links present at 1280x900; Backlog absent from the navbar; Coverage reachable in the mobile sidebar at 360x780; all six v1 routes still answer 200
- **M07** `make check && npx playwright test && screenshot sweep` → all 9 stages; 545 tests; 32 journeys in 10.3 s; overflow 0 at every viewport; commit 64c865e; tree clean
- **N00** `npx vitest run apps/api/tests/personal-store.test.ts` → the canonical index still refuses DELETE, UPDATE and CREATE TABLE with a readonly error while still reading 11 concepts; none of the seven personal tables exists in the canonical index; the personal database accepts a transaction and rolls a failed one back completely; and neither /api/personal/status nor /api/health names the store path
- **N01** `npx vitest run apps/api/tests/personal-store.test.ts` → fresh creation reaches version 1; reopening is a no-op and keeps the data; an unversioned database upgrades and a second run changes nothing; a database written by a newer build is refused with advice to upgrade the application rather than downgrade the data; a migration blocked by a pre-existing table rolls back and leaves user_version at 0 with the old table untouched; and a foreign key violation is genuinely enforced
- **N02** `npx vitest run apps/api/tests/personal-store.test.ts` → CRUD with archive and restore passes; an empty title, an over-long title, an over-long description and an unknown field are each rejected; an unknown project id throws not-found; and the contents counter is correct
- **N03** `npx vitest run apps/api/tests/personal-store.test.ts and apps/api/tests/personal-api.test.ts` → an ordinary Ask request plus seven canonical GET requests leave every personal count at zero; an explicit create produces exactly one session; two sessions in one project stay isolated; and a session id from another project is refused for update, archive and note attachment
- **N04** `npx vitest run apps/api/tests/personal-store.test.ts` → a body containing raw HTML round-trips unchanged; every link target works and an empty one stays null; an empty body, a whitespace-only body, an over-long body and a malformed concept id are rejected; a saved item from another project is refused; edit preserves createdAt; archive and restore work; and filtering by concept works
- **N05** `npx vitest run apps/api/tests/personal-store.test.ts` → upsert keeps one row and replaces the level; clear removes it and reports whether anything was removed; a level outside the four, a malformed concept id and an over-long note are rejected; an unknown concept is refused when the corpus is known; and listing by level and the map used for path building are correct
- **N06** `npx vitest run apps/api/tests/personal-store.test.ts` → all six types round-trip; saving the same concept twice returns the existing item while two answers stay two items; an unknown field, a fabricated canonical id, a one-concept comparison, an unknown item type and a file:// source URL are each rejected; archive, restore and un-archive-on-repeat-save behave; and a saved answer keeps its result and citations verbatim with no context unless the researcher chose to keep it
- **N07** `npx vitest run apps/api/tests/personal-api.test.ts` → 16 tests passed — full project lifecycle; invalid bodies, unknown fields, malformed ids and unknown ids each with the right status; DELETE is 404; cross-project access to a session, note and saved item is 404; sessions, notes and saved items round-trip with dedup; archive and restore for every child type; four invalid list filters rejected; familiarity set/read/list/clear; a concept outside the corpus refused; and with the store unavailable every personal route is 503 with a reason and no path while canonical routes still answer 200
- **N08** `npx vitest run apps/api/tests/personal-api.test.ts (the redaction audit)` → eleven unique sentinel strings were written through create, update, archive, list and three deliberate failure paths plus an assistant query, and the captured log was searched for every one: none appears. The log does contain the route and the status code, which is what it is for.
- **N09** `make check && npx playwright test && make security-review && git status` → 627 tests; 32 journeys; security 22 pass 0 fail 1 note; commit d9e468a; tree clean; no .db file tracked
- **O00** `npx playwright test tests/browser/workspace.spec.ts` → the page states the boundary in those words; neither "personal.db" nor "/private" appears anywhere in the rendered body; the nav link is reachable and activatable by keyboard; and the empty state explains a project and offers the form
- **O01** `npx playwright test tests/browser/workspace.spec.ts` → one journey creates a project with a description, renames it, confirms the URL carries the project id, archives it through the confirmation dialog, finds it under Archived projects, restores it and finds it back in the chooser
- **O02** `npx playwright test tests/browser/workspace.spec.ts` → all six sections are present and every one has a meaningful empty state: no sessions, no notes, nothing saved, no uploaded context, and an export section explaining that this is the only data that cannot be rebuilt
- **O03** `npx playwright test tests/browser/workspace.spec.ts` → a note added from a concept page survives a reload, appears in its project with a link back to the concept, and leaves the corpus hash identical before and after. A note containing <img src=x onerror=alert(1)> and **bold** renders as exactly those characters, with zero img and zero strong elements created.
- **O04** `npx playwright test tests/browser/workspace.spec.ts` → visiting a concept page twice leaves the level at Not set and the panel says "only you set it"; setting Working shows its meaning and survives a reload; Not set clears it in one action; and the same control is present on a graph-only identity
- **O05** `npx playwright test tests/browser/workspace.spec.ts` → opening two concepts leaves the Saved section empty; one click saves exactly one item; reloading shows it already saved; and the project holds exactly one item, not two
- **O06** `npx playwright test tests/browser/workspace.spec.ts` → an unsaved answer is gone after a reload; asking again and saving puts it in the project with its question visible; the panel states that context is not kept unless the box is ticked; and the answer section says "Nothing was stored" before any save
- **O07** `npx playwright test tests/browser/workspace.spec.ts` → two sessions in one project both appear and reopen in the same order after a reload; the section states that asking a question never creates one; and an answer can be attached to a named session and saved
- **O08** `make check && npx playwright test && make security-review` → 627 tests; 46 journeys; security 22/0/1; commit dff35b5; tree clean
- **P00** `npm install --workspace @navigator/api, npm ls, npm run typecheck, npx vitest run` → both versions recorded exactly in apps/api/package.json; npm ls exits zero; typecheck clean; 627 tests passing under Node 22
- **P01** `npx vitest run apps/api/tests/artifacts.test.ts` → create, deduplicate across and within projects, archive, restore, un-archive on repeat upload, cross-project refusal and malformed metadata all pass; a listing carries no extracted text; and a schema check asserts the artifacts table has no bytes, blob, content or data column
- **P02** `npx vitest run apps/api/tests/artifacts.test.ts` → every allowed text extension round-trips; a Python fixture containing os.system is stored as text and the file it would have created does not exist; invalid UTF-8 warns rather than fails; extraction caps at 200,000 characters and says so
- **P03** `npx vitest run apps/api/tests/artifacts.test.ts and tests/browser/artifacts.spec.ts` → a fixture whose output cells contain a base64 PNG and a credential-shaped sentinel yields neither in the extracted text; a raw cell is skipped and counted in a warning; malformed JSON, a missing cell list and a notebook with no text are each refused. The browser journey repeats the sentinel check through the real upload path.
- **P04** `npx vitest run apps/api/tests/artifacts.test.ts` → a hand-written two-page PDF extracts with page markers; an image-only PDF is refused as scanned; a malformed PDF is refused; a 303-page PDF stops at 300 and says so; and the extraction budget stops a long PDF partway through
- **P05** `npx vitest run apps/api/tests/artifact-upload.test.ts` → 16 tests passed — success, label fallback, notebook output discarded, deduplication, metadata-only listing, deliberate text fetch, archive and restore; unsupported extension, over-limit file, binary content, empty file, malformed notebook, type mismatch, non-multipart request, unknown project, cross-project access and DELETE all refused; and with the store unavailable every artifact route is 503. A sentinel audit drives three uploads including two failures and proves the file name, the label and the contents never reach the log.
- **P06** `npx playwright test tests/browser/artifacts.spec.ts` → 8 journeys passed — the explanation is present before a file is chosen; a Markdown file uploads, shows its metadata, previews, archives and restores; a notebook uploads with its output-discarded warning and none of its output in the preview; an unsupported file is refused with a reason and nothing is stored; and the same bytes twice stay one artifact
- **P07** `npx vitest run apps/api/tests/private-context.test.ts` → 17 tests passed — with no selection the prompt contains none of six sentinels; with a selection it contains exactly the selected two and none of the unselected, the other project or the archived one; a selection without a project is ignored; retrieval returns identical canonical concepts with and without a selection; and the budget shortens an over-long item, records an item it had no room for, and preserves the researcher ordering
- **P08** `npx vitest run apps/api/tests/private-context.test.ts and npx playwright test tests/browser/artifacts.spec.ts` → the citable ids contain no artifact or note id; the evidence list and the whole response body contain no private text; the prompt says NOT citable; and in the browser the two sections are separate elements, the private one says it is not canonical evidence and not cited above, and no citation link names the uploaded file
- **P09** `make check && npx playwright test && make security-review && docker build context assertion` → 686 tests; 54 journeys; security 21/0/2; no private path in the build context; commit 2820047; tree clean
- **Q00** `npx vitest run --root packages/core tests/compare-paths.test.ts` → 26 passed — headings, missing sections, math and code fences preserved verbatim, Tier 2 no-section, Tier 3 no-article.
- **Q01** `npx vitest run --root apps/api tests/compare-paths.test.ts` → 24 passed — 2/3/4 concepts, duplicates, unknown ids named in the 404, Tier 3 no-article cells, empty vs absent sections, index unavailable 503.
- **Q02** `npm run test:browser -- tests/browser/compare.spec.ts` → 10 passed — two and four concepts added, compared and removed from the keyboard alone at desktop and 360px, every cell present at both widths, document never wider than the viewport.
- **Q03** `npx vitest run --root apps/api tests/compare-paths.test.ts` → A fabricated citation returns synthesis: null with code fabricated_citation and the full comparison; an unparseable reply does the same with invalid_model_output; the prompt contains the table and no section outside the comparison contract.
- **Q04** `npx vitest run --root packages/core tests/compare-paths.test.ts` → 28 passed — direct, multi-hop, tied (declaration order ignored), cyclic, unreachable, unknown target, Tier 3 step, and an empty known set identical to no known set.
- **Q05** `npx vitest run --root packages/core tests/compare-paths.test.ts` → Five familiarity cases pass, including that the target itself is never treated as known.
- **Q06** `npm run test:browser -- tests/browser/path.spec.ts` → 5 passed — reachable ten-step route built from the keyboard, unreachable target stating the corpus records no route, a personalised route dropping a strong concept and naming the record, and a graph-only step marked as having no article.
- **Q06** `npm run test:browser` → 69 passed — the whole browser suite, including every v1 journey, with Compare and Path added to the navigation.
- **Q07** `npx vitest run --root packages/core tests/export.test.ts && npx vitest run --root apps/api tests/export-cli.test.ts` → 12 + 8 passed — the saved shape accepts exactly what the comparison and path engines produce and rejects an unknown field; the export carries every id, source URL, review state and missing marker, is byte-identical when nothing changed, and the corpus hash is unchanged after exporting.
- **Q07** `npm run test:browser -- tests/browser/path.spec.ts tests/browser/compare.spec.ts` → 16 passed — a comparison and a route are each saved to a project from the keyboard, and the workspace shows the exact export command for each.
- **Q08** `npm run check && npm run test:browser && bash scripts/security-review.sh` → 9/9 stages, 758 unit and API tests, 69 browser journeys, security review 24 passed 0 failed 1 documented note; two new review checks assert the exporter opens the private store read-only and defaults to .navigator/exports.
- **R00** `npx vitest run --root packages/core tests/proposal.test.ts` → 43 passed — unknown fields, absolute paths, parent traversal, backslashes, control characters, a tier writing the other tier's file kind, every invalid transition, and a base commit absent from the repository (tested in a temporary git repository, including a tree that is not a commit).
- **R01** `npx vitest run --root packages/core tests/proposal-request.test.ts && npx vitest run --root packages/core tests/cli.test.ts` → 19 + 27 passed — the same target, tier and base commit produce a byte-identical brief, with the proposal id and the created time masked; a second prepare on the same day gets a new id rather than overwriting; tier 1, an unknown id and an unaddressable label are all refused.
- **R02** `npx vitest run --root packages/core tests/proposal-validate.test.ts` → 19 passed in a temporary git repository holding a copy of the real corpus — unrelated files, a path climbing out of the tree, a deletion, review promotion on an existing page and on a new one, a changed concept id, invalid content, a pasted key, a missing and a near-empty summary, an empty patch, an unappliable patch, and an absent base commit; lowering to disputed-or-conditional is allowed.
- **R03** `npx vitest run --root packages/core tests/proposal-accept.test.ts` → 14 passed in a temporary git repository — every guard refused from the failing side, a dry run that changes nothing, a real acceptance leaving the change staged and uncommitted on a new branch, no new commit anywhere, acceptance refused twice, and rejection recording a reason while applying nothing.
- **R04** `npx vitest run --root packages/core tests/hermes-profile.test.ts` → 26 passed — the audit finds every required prohibition, output and check, and finds no permission anywhere for an agent to accept, approve or apply its own work, including no flag and no way to fake a review by editing the bundle.
- **R05** `npx vitest run --root packages/core tests/hermes-detect.test.ts` → 15 passed against fake executables — full success, a binary that is not installed, each missing subcommand, each of the five missing flags named individually, a kanban create that cannot be asked about itself, and a partial build still reporting what it did find. The source is audited for install, update and configure commands and for shell use.
- **R06** `npx vitest run --root packages/core tests/hermes-task.test.ts` → 13 passed — no board, project or task is created; hostile proposal text containing shell metacharacters, quotes and injected flags stays one argument, and running the printed command through a real /bin/sh against an argv-reporting fake produces exactly the argument vector the adapter would dispatch.
- **R07** `npx vitest run --root packages/core tests/hermes-execute.test.ts` → 12 passed against a fake that logs every invocation — a missing and a wrong confirmation, a dirty repository, missing setup, a proposal past dispatching, a nonzero exit leaving it prepared, an unreadable reply that still moves it to running with a warning, a refused second dispatch, five reply shapes for the task id, and a log showing five read-only probes then exactly one create.
- **R08** `npx vitest run --root packages/core tests/proposal-import.test.ts` → 11 passed on real git worktrees of a temporary repository — a valid result reaching review with a passing verdict, a foreign clone refused, a committed worktree refused, a missing RESULT.md refused, a worktree with no change refused, a proposal past review refused, and a dirty unrelated path imported to review carrying the reason it fails. Ignored paths never enter the patch, and neither tree is committed to.
- **R09** `npx vitest run --root packages/core tests/readme-workflow.test.ts` → 12 passed — every npm script named in the README exists, every navigator subcommand is one the CLI dispatches, every adapter and CLI flag is one they offer, the eight steps appear in order, every relative link resolves to a file and every anchor to a heading, and nothing documents an automatic restart.
- **R10** `npm run check && npm run test:browser && bash scripts/security-review.sh && git ls-files | grep -cE '.navigator/|worktree-|task_'` → 9/9 stages with 946 unit and API tests, 70 browser journeys, security review 27 passed 0 failed 1 documented note, and zero tracked files matching a proposal workspace, an agent worktree or a task id. Three new review checks assert the Hermes adapter never installs, updates, configures or restarts anything, spawns only through an argument array, and that no bundle is tracked.

## Blockers

_none_

## Important decisions

- Host toolchain is unchanged from v1: Node v22.14.0 from `/Users/nick/.nvm/versions/node/v22.14.0/bin`, which every build command puts on `PATH` first because the host default `node` is v26 and the project pins `engines: >=22 <23`.
- J02 content baseline — SHA-256 of each canonical page, frozen so K08 can prove the richer v2 model rewrote nothing: backpropagation-through-convolution ac5e64a3, convolution b2bf2651, convolutional-layer 76156beb, cross-correlation 1429387d, lenet 76c55072, pooling bf2e934d, receptive-field 6c361e06, residual-connection e341c713, resnet e04f7a1c, translation-equivariance 2423c311, vgg 9e27b75a. Full 64-character digests are in the scratchpad file j02-content-hashes.txt and are reproducible with the command above.
- Prettier checks every tracked Markdown file, so V2_BUILD_STATE.md is kept prettier-clean (blank line after each heading, underscore emphasis). The scratchpad helper that maintains it emits that shape directly rather than relying on a later formatting pass.
- V2_BUILD_STATE.md is listed in .prettierignore alongside BUILD_STATE.md rather than kept prettier-clean by hand. The two state files are appended to by a helper on every checkpoint, and a formatter fighting that helper would fail make check for a reason unrelated to the product.
- generated/graph.json is tracked and carries a builtAt timestamp, so any local compile shows it as modified. That churn is expected and is committed at phase boundaries rather than suppressed; the corpus hash beside it is the field that actually means something, and it is unchanged.
- Atlas category paths resolve in two forms. Each category registers its full display path (Artificial Intelligence/Domains/Computer Vision) and, when the title is unique inside its area, the short Area/Title form (Artificial Intelligence/Computer Vision). The eleven v1 pages were written before the atlas existed and use the short form, and freezing Appendix A means Computer Vision genuinely sits under Domains. Registering both keeps the frozen hierarchy and the frozen content true at once, with no change to either. A duplicated title inside one area is rejected outright, so the short form is never ambiguous.
- content/atlas.yaml was generated by a scratchpad script that reads Appendix A out of the runbook rather than retyping it. The script is not tracked; the durable guarantee is packages/core/tests/atlas-seed.test.ts, which re-parses Appendix A in TypeScript and asserts the atlas matches it label for label. Two independent parsers agreeing is stronger evidence than a generator nobody re-runs.
- Claim-level evidence is required for review_state source-checked, expert-reviewed or formally-verified, full stop, rather than only for pages "created or modified after v2". Git history is not available to the validator, and no page has ever been promoted, so the two readings agree today; making it unconditional means the rule cannot be evaded later by editing an old page in place.
- DOTTED_ID was relaxed so that segments after the first may begin with a digit: ^[a-z][a-z0-9_]*(\.[a-z0-9][a-z0-9_]*)+$. The v2 section 4.4 worked example uses source_id paper.resnet.2015, which the v1 regex rejected, and CONCEPT_SLUG already allowed a digit-leading name segment such as /concepts/3-sat that no concept_id could then match. The first segment still must start with a letter, so no id begins with a digit, and no existing id changed. Two new schema tests pin both halves of the rule.
- The corpus hash covers canonical identities only — Markdown pages and graph-only YAML — and the atlas gets its own atlasHash. The corpus hash means "what the corpus says", and v1 documents it that way (README backup/recovery compares it); folding editorial structure into it would make an atlas edit look like a knowledge change. Both hashes are reported by navigator validate and both are compiled into build_meta.
- V1 tests that asserted the literal schema version 1 were updated to assert the SCHEMA_VERSION constant, which L00 requires to be 2. Six assertions across four files changed; each now pins the constant rather than a number, so a future bump cannot silently pass. The index handle still refuses anything newer than the constant, which is what the rule protects.
- Identity routes are addressed by the slug last segment (/identity/resnet), not by the dotted concept id the runbook names. A path segment containing dots is read as a file name by ordinary static servers: docusaurus serve returned 404 for /identity/concept.deep_learning.resnet even though the file existed, and the page then hydrated from 404.html, producing a React hydration mismatch. The schema forces the slug last segment to match the last segment of the concept id and slugs are unique, so this is a one-to-one encoding of the same identity rather than a second, weaker address. The panel still resolves a dotted concept id if a server ever delivers one, and a browser test now pins that every identity URL answers 200.
- The personal-data Docker volume and the /private directory in Dockerfile.api were added at N07 rather than waiting for S00. Without them the container would open the personal store at a path it cannot write for the whole of phases O through R, leaving the product unusable at every phase boundary in between, which execution rule 11 forbids. S00 still runs and re-verifies that only the API mounts the volume.
- CORS was widened from GET/POST/OPTIONS to include PATCH, for the personal update routes. DELETE is deliberately still absent: nothing in this product hard-deletes a researcher work, so there is no method for it to allow.
- The security review was strengthened rather than adjusted at N08. Its "no filesystem write in the API" check pinned a pattern list that never included mkdirSync, so it would have passed a real write; it is now call-shaped, covers the *Sync variants, and excludes only apps/api/src/personal, with four further checks that the private store never names a canonical path, that no database is opened outside it, that the assistant reads nothing from it, and that only the API mounts its volume. Its redaction check pinned two literal strings; it now asserts all eighteen PRIVATE_FIELDS entries and that the paths are derived from that list. 15 checks became 23.
- The browser suite gives each run its own personal database under a per-run temporary directory, set in playwright.config.ts. Deleting the file in a global setup does not work: the API already holds it open, and SQLite keeps writing to the unlinked inode, so the reset silently did nothing and the first workspace journey saw the previous run work.
- Two product defects were found by the workspace journeys and fixed. The Archived projects panel loaded once on mount, so archiving a project left it absent from both lists until a manual reload; it now reloads on a token bumped by archive and restore. And the create-project form is behind a New project disclosure once a project exists, which the journeys now open the way a researcher would.
- The extraction timeout had to be enforced inside the work, not only by a racing timer. Page extraction is CPU-bound and resolves through microtasks, so a setTimeout never got a turn and a 1 ms budget completed a 60-page PDF in full. The PDF page loop now checks a deadline on every page, and the timer is kept as a backstop for work that yields and then hangs. The test that caught it is pinned.
- Browser journeys share one private store per API within a run, so the "no projects yet" journey now runs against the generation-off API on port 8102, whose store no other journey writes to. That keeps the assertion strict rather than conditional, and additionally proves the workspace works with generation switched off.
- The graph-only path journey is served from a stubbed /api/paths response. The acceptance corpus deliberately holds no Tier 3 identity and the coverage journeys assert that count is zero, so adding one to make a journey pass would change the corpus to suit a test. The API behaviour is proven end to end against a compiled mixed-tier corpus in apps/api/tests/compare-paths.test.ts; the browser journey asserts only what the page does with such a step.
- The Path page keeps every concept title it has seen rather than deriving labels from the current route. A concept marked as known leaves the route, and the chip offering to put it back was showing a raw dotted id — the browser journey caught it.
- The comparison and path saved payloads now require the whole structure rather than a summary: the schemas moved to packages/core/src/export.ts and apps/api/src/personal/saved.ts imports them. The exporter runs in the command line with no API behind it, so one definition is the only way a saved item and an exported one cannot drift. Two N06 fixtures were rewritten to the fuller shape; the strictness and round-trip assertions they make are unchanged.
- prepare refuses to derive an address from a label that would lose a character. A* would have become /concepts/a and concept.search.a — a permanent address that does not say what the label says. The preparer now asks for --name instead, and labelIsAddressable draws the line at letters, digits, spaces and dashes.
- The concept id segment is derived from the innermost category in either of the two forms a gap arrives in: an atlas category id (atlas.artificial_intelligence.deep_learning) or the display path an unresolved reference proposes (Artificial Intelligence/Deep Learning). Both produce deep_learning, so the same idea gets one address whichever list it was noticed in.
- accept runs content validation and a compile into a temporary database as its focused tests, rather than shelling out to npm test. The check has to work in any checkout of the repository, including the temporary ones the integration tests use, and it is the corpus-level check that a content change can actually break. The command then tells the person to run npm test before committing, and commits nothing itself.
- A dispatch where hermes exits zero but its reply cannot be parsed still moves the proposal to running, with the problem reported and no task id recorded. Hermes accepted the task, so the work is under way; leaving the proposal prepared would invite a second dispatch of the same work. The alternative — claiming success with an invented id — is worse than saying plainly that the board has to be checked.
- RESULT.md is excluded from the computed patch. An agent writes it at the root of its worktree, where git add -A would stage it, and importing it as content would add the agent's own account of its work to the corpus. It is copied into the bundle instead — so a worktree holding only a result has produced nothing to review, which is exactly what the import then says.

## Next action

S00 — audit the personal data volume.

# Knowledge Navigator Version 2 Build State

Resume point for any agent continuing the Version 2 build.

**Build authority:**
[`KNOWLEDGE_NAVIGATOR_V2_BUILD_RUNBOOK.md`](./KNOWLEDGE_NAVIGATOR_V2_BUILD_RUNBOOK.md).
That file — not this one — holds the v2 product contracts (§4), the frozen
decisions (§3), every v2 checkpoint, and the v2 Definition of Done (§17). Frozen
decisions are deliberately **not** copied here so there is one authority and no
drift.

**Version 1 evidence lives in [`BUILD_STATE.md`](./BUILD_STATE.md) and stays
there.** It must never be copied wholesale into this file. V1 is a finished
dependency: read it to learn what already works and why a decision was taken,
and record here only what Version 2 adds, changes, or re-verifies.

**Project root:** `/Users/nick/Projects/knowledge-navigator` (the folder holding
both runbooks). The build never reads or writes parent directories.

**How to resume:** read the v2 runbook in full, then the v1 runbook,
`BUILD_STATE.md`, `README.md` and `AGENT_CONTENT_CONTRACT.md`, then this file.
Start at the first checkpoint not listed under _Completed checkpoints_,
honouring its **Depends on** line.

## Current checkpoint

K08

## Completed checkpoints

- **J00** — Inventory. Project root `/Users/nick/Projects/knowledge-navigator`, branch `main`, HEAD `0b454cf` ("Definition of Done: all 33 statements verified"). `BUILD_STATE.md` reads `COMPLETE`. Working tree held exactly one untracked file, `KNOWLEDGE_NAVIGATOR_V2_BUILD_RUNBOOK.md`, which the checkpoint permits. Toolchain: Node v22.14.0 (nvm), npm 10.9.2, Docker 28.5.2 with Compose v2.40.3, sqlite3 3.51.0, GNU Make 3.81, Python 3.14.6. The v1 Compose stack was running and healthy (api and web; only web published, on 127.0.0.1:3000). No unrelated user files are at risk and nothing outside this folder is read or written.
- **J01** — `V2_BUILD_STATE.md` created with the six required sections plus the pointer back to `BUILD_STATE.md` for v1 evidence.
- **J01** — V2_BUILD_STATE.md created with the six required sections, a pointer to the v2 runbook as authority, and an explicit statement that v1 evidence stays in BUILD_STATE.md and is never copied here.
- **J02** — V1 regression baseline captured before any v2 change. make check 9/9 stages in 12 s with 330 unit tests across 16 files; 7 browser journeys passed in 4.9 s; smoke test 16 checks passed with the stack torn down and the named volume preserved. Corpus hash bb96930aab75dd2c3741c91919f65b8ba3ea53b5a7c13cea1588c7fd1edff96e over 11 Tier 1 pages, 96 internal links, 19 distinct sources, 40 citations, 0 errors. The eleven per-file SHA-256 hashes are pinned below for K08 to compare against.
- **J03** — Version 2 local-data boundaries added to .gitignore, .dockerignore and .prettierignore. Git and the Docker context now exclude .navigator/ (proposal workspaces and private exports), data/personal.db and its WAL/SHM siblings, data/uploads|artifacts|extraction/, *.upload.tmp, *.extract.tmp, exports/ and *.navigator-export.json. The Docker context additionally excludes the v2 runbook and V2_BUILD_STATE.md, matching how the v1 pair is treated. content/atlas.yaml and content/graph-only/ are deliberately NOT ignored by either boundary, because the API image compiles them.
- **J04** — content/graph-only/ created with a .gitkeep and staged. No Tier 2 or Tier 3 content invented.
- **J05** — Phase J committed as 1ab96ce. Staged diff inspected: seven files, none of them a database, an environment file, a private export or a proposal workspace; every credential-pattern hit in the diff was the v2 runbook prose describing the checks that look for credentials. Working tree clean.
- **K00** — Atlas schema implemented in packages/core/src/atlas.ts against v2 runbook section 4.1: Zod strictObject schemas for the document, areas, categories and candidates, plus validateAtlasStructure for every rule Zod cannot express. A candidate has no field for a factual summary, and writing one is refused by name. Diagnostic and sortDiagnostics moved to a new packages/core/src/diagnostics.ts so the atlas, graph-only and corpus validators can share them without an import cycle; validate.ts re-exports both so no existing caller changed. ATLAS_STATUS_TRANSITIONS makes the editorial state machine explicit: a covered candidate may only fall back to candidate, never be deferred or re-proposed.
- **K01** — content/atlas.yaml seeded from Appendix A: 3 areas, 38 categories, 291 candidates, 11 covered, 280 uncovered, 8 deliberately empty categories. Appendix A names 331 labels of which 329 are distinct after normalisation; the two repeats, Lean and Logic Programming, each became ONE candidate filed in both of its categories, exactly as the contract requires. No candidate carries a summary, a source or a relationship.
- **K02** — Graph-only Tier 3 identities implemented in packages/core/src/graph-only.ts. The schema IS conceptFrontmatterSchema narrowed to tier 3, so every identity and relationship rule is shared rather than copied; the module adds only the storage contract — one YAML document, no Markdown body, file name derived from the slug. LoadedConcept gained a format discriminator (markdown | graph-only) so corpus validation and compilation handle both formats through one path.
- **K03** — conceptKinds extended with paper, person, historical-event, dataset and benchmark. The twelve v1 kinds keep their spelling and their order, so no existing page changed. No instance of an added kind was created.
- **K04** — unresolved_references added to the shared frontmatter schema, so Tier 1, Tier 2 and Tier 3 all carry it. Labels are normalised with the same function used for concept names; duplicates within one identity are rejected, as is a label that is the identity own title or alias. Sections are validated against the template vocabulary and must be empty on a graph-only identity, which has no sections. Corpus rules add two more: a proposed category must resolve in the atlas, and a label the corpus now answers to is reported as stale rather than left in the backlog. unresolvedReferenceId() and backlogGroupId() give each entry a deterministic id and each label one group.
- **K05** — claims and evidence implemented per section 4.4. Claim status is supported | conditional | disputed | unsupported. Evidence carries a human-readable locator and an optional note, and its source_id must appear in that identity own sources list. Claim ids are unique within a page and across the corpus. Status and evidence must agree: supported, conditional and disputed each need at least one piece of evidence, and unsupported must have none. Section splitting moved into packages/core/src/sections.ts (with TIER_1_HEADINGS extracted to headings.ts) so the promotion rule can ask which sections actually have prose.
- **K06** — loadCorpus now loads Markdown pages, graph-only YAML identities and the atlas together and validates them in one pass. File ordering is by name within each format and by concept id in the result. CorpusResult gained atlas, atlasHash and a CoverageSummary, all reported separately from the canonical identities so a candidate is never mistaken for a concept. New corpus rules: a covered candidate must name a concept that exists, at most one candidate may cover a concept, and a candidate whose label already names a concept must be marked covered. A missing graph-only directory or atlas file is not an error, and atlas-dependent rules are skipped when no atlas is supplied.
- **K07** — Three JSON Schemas are now generated from the same Zod definitions the runtime uses: schemas/concept.schema.json, schemas/graph-only.schema.json and schemas/atlas.schema.json. navigator schema writes all three and --out now names a directory. AGENT_CONTENT_CONTRACT.md was rewritten for the v2 model: canonical versus candidate, the four coverage depths, the five added kinds, the graph-only file shape, unresolved references, claims and evidence, the shared section vocabulary, and four new checklist lines.
- **K08** — V1 compatibility proven. Validation reports 11 concepts, all Tier 1, all generated-draft, all markdown format, zero Tier 2, zero Tier 3, zero errors, corpus hash bb96930a unchanged from the J02 baseline. All eleven canonical files are byte-identical to their J02 hashes, and git shows no modification under content/concepts.

## Last successful checks

- **J00** `pwd && git rev-parse HEAD && git status --short && docker compose ps -a` → root correct; HEAD `0b454cf`; only the v2 runbook untracked; api and web both healthy.
- **J01** `read V2_BUILD_STATE.md as a fresh agent would` → Current checkpoint, Completed checkpoints and Next action together identify J02 without reference to anything but the two runbooks.
- **J02** `make check` → all 9 stages passed in 12 s; 330 tests in 16 files; graph.json and sidebars.generated.ts byte-identical across two compiles
- **J02** `npm run test:browser` → 7 journeys passed in 4.9 s
- **J02** `./scripts/smoke.sh` → 16 checks passed; containers torn down, navigator-data volume preserved
- **J02** `shasum -a 256 content/concepts/*.md` → digest of the eleven hashes: 3825f5e70952be855003b3f6d0afc8af5cf3d4c144c1955538090d12692041df (list recorded under Important decisions)
- **J03** `git check-ignore over 15 private paths and 11 canonical v2 paths` → all 15 private paths ignored (proposal manifest, private export, personal.db and -wal/-shm, uploads, artifacts, extraction temporaries, external export, .env, knowledge.db, node_modules); all 11 canonical paths trackable (content/atlas.yaml, content/graph-only/*.yaml, concept Markdown, the six new core modules, the personal API module, the new web pages, the Hermes script, .env.example, package-lock.json, generated/graph.json, sidebars.generated.ts)
- **J03** `docker build against a busybox stage that asserts on the real build context` → exit 0 — .navigator/, data/personal.db, both v2 authority documents, .git, .env, data/ and node_modules are absent from the context, while content/concepts/resnet.md, package.json and AGENT_CONTENT_CONTRACT.md are present. A real .navigator/proposals/p1/manifest.json and data/personal.db existed on disk during the test and neither reached the context.
- **J04** `npm run validate` → 11 concepts, 11 Tier 1, 11 generated-draft, 4 categories, 20 relationships, 96 internal links, 19 sources, 40 citations, corpus hash bb96930a unchanged, 0 errors
- **J05** `git add -A && git diff --cached --name-only && credential scan && git commit && git status --short` → commit 1ab96ce; staged .dockerignore, .gitignore, .prettierignore, the v2 runbook, V2_BUILD_STATE.md, content/graph-only/.gitkeep and generated/graph.json; tree clean afterwards
- **K00** `npx vitest run packages/core/tests/atlas.test.ts` → 40 tests passed — one valid atlas plus one failing case for each rule (unknown key, factual summary, wrong schema_version, malformed id, bad status, no categories, malformed YAML, empty file, wrong area set, duplicate area/category/candidate id, id collisions, missing parent, self-parent, cycle, cross-root parent, duplicate sibling title, missing category, candidate on a root area, repeated category, normalised title and alias collisions, covered without a concept id, non-covered with one). A document breaking four rules at once returns all four in a stable order, identical across two runs.
- **K01** `npx vitest run packages/core/tests/atlas-seed.test.ts` → 9 tests passed — the shipped atlas is structurally valid; the three root areas are present; every Appendix A label is represented exactly once after normalisation (re-derived from the runbook by the test itself, independently of the generator); Programming has 5 categories and 49 candidates with zero canonical pages behind them; exactly the eleven existing concepts are covered and every other candidate is unmapped; empty categories survive; and all four category paths the canonical corpus already uses resolve.
- **K02** `npx vitest run packages/core/tests/graph-only.test.ts` → 21 tests passed — a valid identity loads with no body, headings or links and needs no Tier 1 template; a Markdown body after a separator, an explicit body key, any unknown key, an empty file and unparseable YAML are all rejected; tiers 1, 2 and 4 are rejected; the shared identity rules (dotted id, slug/id agreement, primary_category, self-relationship, atlas area) all fire on YAML; and in a mixed corpus a file-name mismatch, a duplicate concept_id, a duplicate slug, a normalised alias collision with a Markdown page and a dangling relationship target are all caught.
- **K02** `npx vitest run` → 400 tests passed across 19 files — the 330 v1 tests plus 70 new ones; no v1 test changed
- **K03** `npx vitest run packages/core/tests/schema.test.ts && npm run validate && diff of content hashes against the J02 baseline` → 49 schema tests passed, including one asserting the first twelve entries are still exactly the v1 list and one that parses a page for every one of the 17 kinds; validate still reports 11 concepts, 11 Tier 1, corpus hash bb96930a unchanged; all eleven per-file SHA-256 hashes identical to the J02 baseline
- **K04** `npx vitest run packages/core/tests/unresolved-references.test.ts` → 25 tests passed — a full entry parses, defaults fill in, the field defaults to empty, Markdown and YAML both accept it; missing/empty label or reason, unknown key, bad section name, repeated section, bad proposed_kind, non-atlas area and non-boolean blocking all fail with a field path; duplicate and self-referential labels fail; the atlas rule fires on an unknown category and is skipped when no atlas is supplied; a stale label is reported; ids are deterministic and always valid dotted segments
- **K05** `npx vitest run packages/core/tests/claims.test.ts` → 33 tests passed — all four statuses, evidence with and without a note, one source supporting several claims; malformed claim_id, section, statement, status, unknown key and missing locator; every status/evidence disagreement; evidence pointing at an uncited source; duplicate claim ids within a page and across two pages; and the promotion rule for all three promoted review states, including a promoted page that covers every section, a section with a heading but no prose, and a promoted Tier 2 page with no claims
- **K06** `npx vitest run packages/core/tests/mixed-corpus.test.ts` → 12 tests passed — a Tier 1 + Tier 2 + Tier 3 + atlas corpus validates cleanly, sorts by concept id across both formats, keeps the atlas out of the concept list, summarises coverage, is deterministic across two loads, includes graph-only files in the corpus hash, and works with no graph-only directory and no atlas; a single run reports a cross-format duplicate id, duplicate slug, normalised alias collision and two broken relationship targets together, and a malformed atlas does not swallow the concept diagnostics
- **K07** `npm run schema twice + npx vitest run packages/core/tests/contract-audit.test.ts` → both generations byte-identical (atlas 4c0b189c, concept 4fe7b293, graph-only 9d51980b); 25 audit tests passed — every concept kind, review state, claim status, section key, atlas status and Tier 1 heading (in order) appears in the written contract, as does every required field of the frontmatter, unresolved-reference, claim and candidate schemas; the files in schemas/ match the generator exactly; all three refuse unknown keys; and the seven v2 boundary statements are present
- **K08** `npm run validate && diff of shasum -a 256 content/concepts/*.md against the J02 baseline && git status --short content/` → 11 Tier 1, 0 Tier 2, 0 Tier 3, 0 errors; all eleven hashes identical; the only new path under content/ is the atlas file itself

## Blockers

_none_

## Important decisions

- Host toolchain is unchanged from v1: Node v22.14.0 from `/Users/nick/.nvm/versions/node/v22.14.0/bin`, which every build command puts on `PATH` first because the host default `node` is v26 and the project pins `engines: >=22 <23`.
- J02 content baseline — SHA-256 of each canonical page, frozen so K08 can prove the richer v2 model rewrote nothing: backpropagation-through-convolution ac5e64a3, convolution b2bf2651, convolutional-layer 76156beb, cross-correlation 1429387d, lenet 76c55072, pooling bf2e934d, receptive-field 6c361e06, residual-connection e341c713, resnet e04f7a1c, translation-equivariance 2423c311, vgg 9e27b75a. Full 64-character digests are in the scratchpad file j02-content-hashes.txt and are reproducible with the command above.
- Prettier checks every tracked Markdown file, so V2_BUILD_STATE.md is kept prettier-clean (blank line after each heading, underscore emphasis). The scratchpad helper that maintains it emits that shape directly rather than relying on a later formatting pass.
- V2_BUILD_STATE.md is listed in .prettierignore alongside BUILD_STATE.md rather than kept prettier-clean by hand. The two state files are appended to by a helper on every checkpoint, and a formatter fighting that helper would fail make check for a reason unrelated to the product.
- generated/graph.json is tracked and carries a builtAt timestamp, so any local compile shows it as modified. That churn is expected and is committed at phase boundaries rather than suppressed; the corpus hash beside it is the field that actually means something, and it is unchanged.
- Atlas category paths resolve in two forms. Each category registers its full display path (Artificial Intelligence/Domains/Computer Vision) and, when the title is unique inside its area, the short Area/Title form (Artificial Intelligence/Computer Vision). The eleven v1 pages were written before the atlas existed and use the short form, and freezing Appendix A means Computer Vision genuinely sits under Domains. Registering both keeps the frozen hierarchy and the frozen content true at once, with no change to either. A duplicated title inside one area is rejected outright, so the short form is never ambiguous.
- content/atlas.yaml was generated by a scratchpad script that reads Appendix A out of the runbook rather than retyping it. The script is not tracked; the durable guarantee is packages/core/tests/atlas-seed.test.ts, which re-parses Appendix A in TypeScript and asserts the atlas matches it label for label. Two independent parsers agreeing is stronger evidence than a generator nobody re-runs.
- Claim-level evidence is required for review_state source-checked, expert-reviewed or formally-verified, full stop, rather than only for pages "created or modified after v2". Git history is not available to the validator, and no page has ever been promoted, so the two readings agree today; making it unconditional means the rule cannot be evaded later by editing an old page in place.
- DOTTED_ID was relaxed so that segments after the first may begin with a digit: ^[a-z][a-z0-9_]*(\.[a-z0-9][a-z0-9_]*)+$. The v2 section 4.4 worked example uses source_id paper.resnet.2015, which the v1 regex rejected, and CONCEPT_SLUG already allowed a digit-leading name segment such as /concepts/3-sat that no concept_id could then match. The first segment still must start with a letter, so no id begins with a digit, and no existing id changed. Two new schema tests pin both halves of the rule.
- The corpus hash covers canonical identities only — Markdown pages and graph-only YAML — and the atlas gets its own atlasHash. The corpus hash means "what the corpus says", and v1 documents it that way (README backup/recovery compares it); folding editorial structure into it would make an atlas edit look like a knowledge change. Both hashes are reported by navigator validate and both are compiled into build_meta.

## Next action

K09 — run the full checks and commit Phase K.

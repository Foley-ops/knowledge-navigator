# Frozen decisions (runbook §2): exact dependency versions, committed lockfile,
# and a Node 22 LTS runtime. engine-strict makes the engines range in
# package.json a real gate rather than a warning.
save-exact=true
engine-strict=true
fund=false
audit=false
